"""The desk: the one conversation from a CSV to a routed context pack, and back after the run.

Today this package holds the pack builder (`handoff.py`). The graph that replaces the interview, the router, and
the chat lands here stage by stage; see docs/desk-redesign.md.
"""
