"""The families: one package per analysis family, and the registry the desk reads them through. A core package (common,
profile, memory, viz, lane) never imports this; it takes the families as an argument or offers a registry the families
fill at import."""
