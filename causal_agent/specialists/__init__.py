"""The built lanes, one package each: dowhy for adjustment, did (pyfixest) for diff-in-diff, rd (rdrobust) for discontinuity.
The desk reaches a lane through causal_agent.families.registry, never through this package."""
