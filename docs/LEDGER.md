# Project Ledger

Single source of truth for project state. Any session restores full context by
reading `docs/product/SYSTEM-CONTRACT.md`, the five PRDs, and this file.
Fable owns this file; Opus reads it and verifies hashes from it.

Statuses: `DRAFT` → `READY_FOR_OPUS` → `IN_PROGRESS` → `REVIEW` →
`ACCEPTED` | `REVISION_REQUIRED` | `FABLE_REVISION_REQUIRED`

## Governing document hashes

Recomputed and updated by Fable at every doc freeze
(`shasum -a 256 docs/product/*.md`).

| Document | SHA-256 | Frozen at (commit) |
|---|---|---|
| SYSTEM-CONTRACT.md | `85f7a1d357aab602a1d0c973a55566e5c79c8e784f2704d87d8be2dafe9f051c` | D-061 revision (31-bit seed), 2026-08-25 |
| PRD-001-kaggle-intake-storage.md | `a69337d3bfeac33cc7c017da357df369e01fe2c00d2b8ade429b84eac6913188` | initial freeze |
| PRD-002-causal-design-harness.md | `82eff2c8d5e1b14537033dc22d24bd03fd40d0825e45a92d6dad40acabeff0b3` | D-055 revision (criterion 39: 3,400), 2026-08-25 |
| PRD-003-runnable-frame-preparation.md | `99513ef5c83dcaf816a31ec909b1ee7acdb683bd8a09062a8cf714dac92cba21` | Amendment 1 (V1-lite) + criterion 38 revision (D-057/D-058), 2026-08-25 |
| PRD-004-estimation-diagnostics-judgment.md | `79bb716eec3574508c1daea8686848ed24b74fef2e016e99467eda4c3fb12642` | initial freeze |
| PRD-005-evidence-visualization-presentation.md | `6f00087cd29cfd052d61b305a3f2fe22333c25cef22d8b2c669b243aad5f5f37` | initial freeze |

## Task board

| Task ID | Title | Owning PRD | Depends on | Model | Status | Doc hashes verified | Notes |
|---|---|---|---|---|---|---|---|
| T-001 | Repo scaffolding, uv lock, budget checker | shared (SC §14, §14.1) | — | single-session (D-008) | ACCEPTED | yes | commit `4f7cbaf`; 154 pkgs resolved; 36 tests; budget `within_budget` |
| T-002 | Shared contract kernel (canonical, hashing, envelopes) | shared (SC §2, §3, §8.2) | T-001 | single-session (D-008) | ACCEPTED | yes | commit `bd6c985`; canonical.py adopted from causal-final-de draft with D-006 fix; 40 tests; maps to EV-SYS-001; budget `within_budget` (shared 158, tests 576) |
| T-003 | OperationalEventV1 + NDJSON emitter | shared (SC §10.1) | T-002 | single-session (D-008) | ACCEPTED | yes | commit `7b96eeb`; 28 event names; 21 tests; maps to EV-SYS-005 |
| T-004 | Artifact-type registry + seeded registrations | shared (SC §3.1) | T-002 | single-session (D-008) | ACCEPTED | yes | commit `99d0132`; registry fail-closed on unsupported_schema; 2 rows seeded (D-012); 11 tests; maps to EV-SYS-001/EV-SYS-006 |
| T-005 | Persistence + §8.2 commit protocol | shared (SC §3, §4, §8) | T-002, T-003, T-004 | single-session (D-008) | ACCEPTED | yes | commit `c2fc732`; 14 dockerized integration tests (postgres:18.6 + MinIO); trace-flush gate deferred (D-020); maps to EV-SYS-001/EV-SYS-004 |
| T-006 | Handoff persistence + acceptance gate | shared (SC §3, §3.1, §8.1) | T-005 | single-session (D-008) | ACCEPTED | yes | commit `ec5db6d`; fail-closed, all codes collected; 11 dockerized tests; maps to EV-SYS-006 |
| T-007 | Intake core: contracts, registrations, archive, profiler | PRD-001 (§5.5–§8) | T-004 | single-session (D-008) | ACCEPTED | yes | commit (see checkpoint log); 6 registry rows appended; 58 tests; maps to EV-P1-003/EV-P1-004 |
| T-008 | Intake capture + catalog + coordinator + handoff | PRD-001 (§4, §9–§13) | T-005, T-006, T-007 | single-session (D-008) | ACCEPTED | yes | commit `e613eed`; spec + amendments D-027..D-038; 34 tests; maps to EV-P1-001/002/005; budget `warning` (intake 1180/1200, coordinator 329/350 — both inside limits, near them) |
| T-009 | Design substrate: shared task envelope, design contracts, registry rows, design schema | PRD-002 (SC §5.2, §6.1, §21; §6, §12, §16–§18) | T-002, T-004, T-005, T-008 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit `2c98730`; spec + Amendment 1 (D-044); 134 new tests, suite 349 green; budget `warning` only on pre-existing intake dims (design 601/2500, shared 982/2500, tests 2678/8000, declarative 641/3000, 28 modules) |
| T-010 | Shared Vertex gateway + LangSmith tracing | shared (SC §10.2–§10.4) | T-009 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 44 new tests + 2 env-gated live smokes; closes the D-020 revisit (flush gate live in ArtifactCommitter, D-045); maps to EV-SYS-003/EV-SYS-005; shared 1425/2500 |
| T-011 | Method-pack manifests, triage, DesignContextManifest, tool surface, entry gate | PRD-002 (§4, §5, §9, §13, §15) | T-009 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 67 new tests, suite 457 green; rethink consumed (make_retrieval_handlers 88→module-level handlers); maps to EV-P2-001/003, EV-SYS-002; design 1492/2500, declarative 1324/3000 |
| T-012 | Validation walls, ask gate, diagnostics, delivery capacity | PRD-002 (§10, §11, §14, §16.3, §13.5) | T-011 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 139 new tests, suite 596 green; rethink consumed (2517→2454 via D-048 move); maps to EV-P2-004/005/006/007, EV-SYS-002; design 2454/2500 — T-013 must budget via further consolidation |
| T-013 | Agents, prompts, LangGraph graph, interrupts, renderer, coordinator, outcome + handoff | PRD-002 (§8, §9, §12.3, §19–§23) | T-010, T-012 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 54 new tests, suite 650 green incl. e2e approved runs + durable resume; unblocked by D-055 (design 3,400); graph split into harness_base/harness_nodes/graph (340/277/~236); maps to EV-P2-001..008; design 3,398/3,400 |
| T-014 | CLI seven commands, runtime composition, live Kaggle adapter | shared (SC §1.1) + PRD-002 §11.1 | T-013 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 67 new tests, suite 717 green; closes D-034; maps to EV-SYS-007; cli 410/500, runtime 421/800; modules 50/50 and largest module 350/350 — both caps fully consumed |

## Decision log

Append-only. One line per decision: date, decision, why.

- 2026-08-24 — Repository initialized with two-terminal Fable/Opus write-ownership enforcement (hooks in `.claude/`). Rationale: mechanical enforcement of the role contract instead of honor-system.
- 2026-08-24 — D-001: Task specs live as one file per task under `docs/tasks/`, referenced from PRDs/contract by section; keeps frozen PRDs stable and hashes per-task.
- 2026-08-24 — D-002: Wave 1 = T-001 (scaffolding) ∥ T-002 (contract kernel); disjoint write sets, T-002 depends on T-001 only for the runnable environment.
- 2026-08-24 — D-003: The §14.1.1 budget checker lives at `tools/budget_check.py`, standard library only, assigned to the tests scope for counting (verification tooling, not production behavior). Open to revision if Opus shows a contradiction.
- 2026-08-24 — D-004: Canonical JSON = UTF-8, code-point-sorted keys, `(",", ":")` separators, NFC strings, no NaN/Inf, None preserved, RFC 3339 UTC timestamps with exactly 6 fractional digits and `Z`; hashes are bare 64-char lowercase hex.
- 2026-08-24 — D-005: Identity fields are non-empty strings ≤200 chars at the envelope layer; stricter per-identity formats are enforced at creation sites, not in shared contracts.
- 2026-08-24 — D-006: Canonicalization raises stable code `duplicate_key_after_normalization` when two mapping keys collide under NFC; silent collapse would break key-order determinism. (Raised by Opus during T-002 drafting.)
- 2026-08-24 — D-007: `payload_locator` is exempt from the 200-char identity cap; its own cap is 1024 chars. (Raised by Opus during T-002 drafting.)
- 2026-08-24 — Enforcement finding: write-guard hooks verified live in fresh sessions (headless probe denied correctly) but inert in sessions that predate the settings file or have not approved project hooks; every interactive session must run /hooks once (or restart and accept the hook prompt) and then verify behaviorally.
- 2026-08-24 — D-008: User dissolved the two-terminal Fable/Opus model. Single session architects and implements; hooks removed from settings (scripts kept inert in .claude/hooks/); ledger/checkpoint discipline retained. T-001/T-002 reassigned to the main session. Budget-checker path exclusions fixed as: docs/**, .claude/**, CLAUDE.md, .gitignore, .python-version, pyproject.toml, uv.lock, README.md.
- 2026-08-24 — D-009: `registries/` is a top-level declarative-scope directory for static registries.
- 2026-08-24 — D-010: Static registries are JSON, not YAML (stdlib-parseable; the pinned stack has no YAML parser and none is added).
- 2026-08-24 — D-011: Canonical component IDs: intake-coordinator, design-harness, preparation-harness, estimation-harness, presentation-coordinator, cli, runtime.
- 2026-08-24 — D-012: artifact-types registry file is append-only; each stage task appends its rows once the owning PRD is read, so no parent-type name is invented ahead of its PRD.
- 2026-08-24 — D-013: `Stage` enum includes `system` for shared/CLI/runtime components acting outside one stage.
- 2026-08-24 — D-014: OperationalEventV1 `versions` keys are the closed set {model, prompt, tool, registry, schema, validator, compiler, renderer}.
- 2026-08-24 — D-015: `token_usage` keys are the closed set {input, output, thinking, total}.
- 2026-08-24 — D-016: The emitter (not the model) enforces name registration and requires non-empty `required_eval_ids` on task./agent./tool./handoff. events.
- 2026-08-24 — D-017: Dev/test object store is MinIO via Docker; src/ depends only on an S3 client Protocol (no boto3 import in production code). Postgres runs as the pinned postgres:18.6 image.
- 2026-08-24 — D-018: Legal run-state transition map fixed from the §4 diagram; completed/failed/failed_observability are terminal at the stage-run level.
- 2026-08-24 — D-019: payload_locator format is `objects/{content_hash}`; the committer rejects disagreement.
- 2026-08-24 — D-020: §8.2's LangSmith flush gate is deferred to the tracing task; commit currently ends at reopen-validation + artifact.committed event. Deliberate, recorded, must be revisited when LangSmith lands.
- 2026-08-24 — D-021: The handoff receiver names its expected outcomes explicitly per seam; "wrong outcome" is seam knowledge, not manifest knowledge.
- 2026-08-24 — D-022: Handoff gate events are caller-built via an event factory so the D-016 eval-ID rule stays satisfiable.
- 2026-08-24 — D-023: Intake-internal registrations use terminal status `committed`; outcome statuses live only on IntakeOutcome. KaggleCapture is restricted, readable only by intake-coordinator.
- 2026-08-24 — D-024: kaggle_ref normalizes to owner/slug; owner/slug or kaggle.com/datasets URL accepted.
- 2026-08-24 — D-025: Profiler hypotheses limited to two bounded deterministic rules (identifier, ±9…±9999 sentinel at ≥1% and min/max), always labelled hypothesis.
- 2026-08-24 — D-026: Archive safety limits are constructor parameters (10k entries, 512 MiB file, 2 GiB total, 200:1 ratio above 1 MiB).
- 2026-08-24 — D-027: IntakeOutcome required parents reduced to [QuestionRecord]; the full chain moves to optional and the coordinator enforces it for usable/partial. An early refusal has no chain to cite; the registry must stay committable fail-closed.
- 2026-08-24 — D-028: Raw bytes (archive, extracted source files) are content-addressed objects without envelopes; catalog.resources rows point at them. Only canonical-JSON payloads get artifact envelopes.
- 2026-08-24 — D-029: PRD-001's `artifacts` table is realized by shared `causal.artifacts`; the `catalog` schema owns runs, datasets, resources, source_field_index, and exactly five narrow views. Run operational state stays in `causal.stage_runs`.
- 2026-08-24 — D-030: Provider field classification is the static registry `registries/kaggle-field-classes.v1.json`; unlisted fields default to `operational` (fail-closed away from model context).
- 2026-08-24 — D-031: Deterministic identities: analysis_id = an-sha16(idempotency_key); dataset_id = kaggle:owner/slug@version; artifact_id = kind:analysis_id:hash16. Payloads carry no wall-clock; envelope timestamps come from an injected clock.
- 2026-08-24 — D-032: v0 semantic-map rules are conservative: column meaning from provider description, missing_sentinel from profiler hypotheses, everything else not_offered. Free-text-to-slot interpretation belongs to PRD-002.
- 2026-08-24 — D-033: Outcome rule — refused per PRD-001 §12; partial when any resource is unreadable/excluded/failed or no semantic field is evidenced; usable otherwise.
- 2026-08-24 — D-034: The live kaggle==2.2.4 adapter is deferred to the CLI/runtime task; T-008 ships the client Protocol and capture layer; tests use frozen fixture clients. Credentials exist only at adapter construction from the runtime secret source.
- 2026-08-24 — D-035: Re-running an incomplete analysis uses a new stage_run_id; deterministic artifact IDs make recommits §8.2 replay no-ops (PRD-001 §13 restart-from-boundary).
- 2026-08-24 — D-036: resources.parse_status is PRD-001's five values (parsed, excluded, unreadable, unsafe, failed); withheld classifications map to excluded with a reason.
- 2026-08-24 — D-037: The producer never persists the handoff manifest (the T-006 gate records at receipt). The coordinator's open_handoff rebuilds it deterministically from analysis_id + intake_outcome_artifact_id and refuses for refused/missing/mismatched outcomes. Measured-fact index rows point at TableProfile artifacts. (T-008 Amendment 1.)
- 2026-08-24 — D-038: Shared docker fixtures live in tests/conftest.py so all test packages reuse them. (T-008 Amendment 2.)
- 2026-08-24 — D-039: PRD-002 wave plan = T-009 (substrate) → T-010 (gateway+tracing, shared) → T-011 (manifests/triage/manifest-compiler/tools/entry gate) → T-012 (validators/ask gate/diagnostics/capacity) → T-013 (agents/prompts/LangGraph/renderer/coordinator/outcome/handoff) → T-014 (CLI/runtime/live Kaggle). Specs freeze one task at a time, immediately before that task's implementation, so later specs absorb earlier findings. Declarative-first rule: method packs, requirement/tool/capacity registries, and prompt templates land in the declarative scope, keeping `design` code within its 2,500 lines.
- 2026-08-24 — D-040: AgentTaskEnvelopeV1, AgentTaskResultV1, ContextRequirementV1, and ClaimV1 live in the shared scope (`src/causal/shared/envelope.py`): SC §5.2/§5.4/§6.1/§16.2 define them across PRD-002–005.
- 2026-08-24 — D-041: Build mode for the PRD-002 wave (user-directed): Opus subagents implement the frozen specs; the main session architects, reviews, runs gates, and commits. The D-008 single-session discipline is retained at the spec/review/commit boundary.
- 2026-08-24 — D-042: The dev machine has no Graphviz binary; the Graphviz 15.1.1 pin binds the future OCI image. The renderer must record the actual local Graphviz version in every output and its tests must skip (not fake) when `dot` is absent. No silent substitution.
- 2026-08-24 — D-043: Session environment check: Kaggle credentials (`~/.kaggle/kaggle.json`) and Google ADC are present; no LangSmith key is visible. Tracing reads `LANGSMITH_API_KEY` from the environment at runtime and the preflight fails closed to `failed_observability` when it is missing, per SC §10.2.
- 2026-08-25 — D-056: T-014 seams. `causal approve-design` gains an optional `--changes-file PATH` (JSON list of strings), required exactly for `changes_requested` — the SC §1.1 flag table gives no carrier for the §22 change texts and the typed schema requires them; recorded as a deliberate one-flag extension. StatusView unified on the CLI's model (runtime imports it). The runtime validates interrupt id/hash/revision against the live checkpointed interrupt payload before consuming an idempotency key; approve fills approved_artifacts from the interrupt's own refs so the §22 binding cannot disagree with the revision under review. Kaggle adapter strips provider exception bodies (they can embed tokens) keeping only the class name. Module count 50/50 and largest module 350/350: both caps are fully consumed — PRD-003 needs either headroom recovered or a cap decision before its first module.
- 2026-08-25 — D-067: Pilot finding 8: live semantic cards failed wall 1 with IDENTICAL near-miss slot keys three times — the exact 12-key COLUMN_CARD_SLOTS vocabulary was never in the prompt, and ValidationIssueV1 carried no message, so a temperature-0 model repeated itself. Fix per T-013 Amendment 6: the semantic template lists the 12 keys verbatim (test binds the list to the constant); ValidationIssueV1 gains additive `detail` (default "") filled by shape_report from the pydantic message (200-char cap) — corrections now say WHY. Events unchanged. The recurring pilot pattern is recorded: every model failure so far was a closed vocabulary the harness knew and never told the model, compounded by detail-free corrections.
- 2026-08-25 — D-066: Pilot finding 7: many-item tasks sent the SINGLE-draft response schema, so the live model returned one bare card and the runner crashed `KeyError: 'items'` at the semantic fan-out. Fix per T-013 Amendment 5: `result_schema(draft, many=)` wraps the payload as a required `items` array when many; a many reply without a list `items` routes through the existing schema_invalid correction path (verified: the new e2e test reproduces the exact KeyError with the guard removed). Deferred, recorded for T-019: `runtime/failures.guard` catches only GatewayError/ObservabilityError — an arbitrary exception still escapes `causal run` as a traceback; broad-exception hardening lands with T-019's runtime dispatch work.
- 2026-08-25 — D-065: Pilot findings 6a/6b/6c (round 4). (a) Live Kaggle metadata nests under `info`; the flat capture contract saw nothing, so a dataset with title/subtitle/description yielded ZERO evidence items — adapter now flattens (runtime +3, T-014 A4). (b) No allowlisted id represented the user's question, so intent claims could not legally cite it — the coordinator appends `ua:question/text` (user_confirmation class) to the EvidenceBundle; the usable/partial status computation moved above the append so provider-semantics rules are unchanged (intake +3, T-008 A4). (c) Prompts never rendered the evidence allowlist nor the authored constants — the D-059 agenttask consolidation was pulled forward: `_invoke`/`_run_task` now live in `shared/agenttask.py` as `TaskRunner` (design 3,398→3,337, shared 1,863), which appends `## allowed_evidence` to every prompt; templates state `registry_version`, per-id answer schemas, and cite-or-hypothesis discipline (T-013 A4). Suite 733 green.
- 2026-08-25 — D-064: Pilot findings 5a/5b. (a) Zero-metadata datasets produced an empty structural manifest — per-column facts lived only in the measured class — so every model task saw an empty column list. `measured_index_rows` now also emits a structural `presence` row per profiled column (intake +2, T-008 Amendment 3); a profiled column's existence is structural fact independent of provider metadata. (b) Models invented requirement ids (`req-treatment-cols-1`) because the prompts never state the registered vocabulary; wall 2 rejected them and corrections could not converge. All five design prompt templates now carry the closed registered id list with usage cues, revised IN PLACE without a version bump — no eval baseline exists yet (T-020 pending); this is the recorded pre-baseline prompt revision (T-013 Amendment 3). A consistency test binds prompt-mentioned ids to the registry.
- 2026-08-25 — D-063: Pilot finding 4 (root cause of correction_exhausted): `_invoke` passed `{"type": "object"}` as the Vertex response schema, so constrained decoding at temperature 0.0 deterministically returned `{}` — the registered draft schema never reached the model, and no scripted test could see it. Live probe confirmed Vertex v1 + google-genai 2.19.0 accept a full `model_json_schema()` dict with `$defs`. Fix per T-013 Amendment 2: new `shared/agenttask.py` `result_schema(draft)` (memoized; result-envelope schema with `payload` replaced by the draft schema, `$defs` unioned, non-identical collision raises) — the planned T-019 agenttask seed; `_invoke` passes it (design delta 0 lines).
- 2026-08-25 — D-062: Pilot finding 3: `GatewayError`/`ObservabilityError` subclass `Exception`, escaped `causal run` as a raw traceback, and left the design-run row stuck `running`. Fix per T-014 Amendment 3 (runtime scope): new `runtime/failures.py` — `guard()` wraps every coordinator invocation; GatewayError → one `blocker.raised` + run `failed` + typed result (exit 4); ObservabilityError → run `failed_observability` + typed result (exit 5), committed artifacts preserved, no blocker (the flush gate's own event covers it). composition.py delegated shared row/blocker helpers (346→330); modules 51/68. Verified: gateway emits only retry events, so the runtime blocker is the failure's single one.
- 2026-08-25 — D-061: Pilot finding 2 (first live Vertex call): `generation_config.seed` is signed INT32; the frozen unsigned-32-bit derivation overflowed it (3,755,233,513 → 400 INVALID_ARGUMENT). SC §10.4 seed row revised to a non-negative 31-bit value; `derive_seed` masks with `& 0x7FFF_FFFF`; both seed fields bound `lt=2**31` (T-010 Amendment 1). Same-task determinism preserved.
- 2026-08-25 — D-060: Pilot finding 1 (first-ever live Kaggle call): Kaggle retired `GetDatasetStatus`; `kaggle==2.2.4` `api.dataset_status` returns 404 for every dataset, so live capture failed `fetch_failed` before download. Fix per T-014 Amendment 2: `LiveKaggleClient.dataset_status` resolves `currentVersionNumber` through `api.dataset_list(search=slug)` with exact-ref match; pin, protocol, and `intake/kaggle.py` unchanged; sanitization path unchanged. Consequence accepted: the capture payload's `dataset.status` records `unknown` — the provider no longer offers a status value.
- 2026-08-25 — D-059: PRD-003 wave plan (user-approved): live PRD-002 pilot first, then T-015 substrate → T-016 Phase A stabilization engine → T-017 Phase B operations/executor/diagnostics → T-018 entry gate/plan compiler/6 walls → T-019 LangGraph harness + runtime/CLI integration + PRD-004 handoff (rethink reserve; design `_run_task` moves to `shared/agenttask.py` via the D-049 pattern) → T-020 `evals/catalog.v1.yaml`. Shared reallocation (~370 lines: receipts, frame I/O, agent-task runner) under SC §14.1 shared responsibilities. Specs freeze one at a time; Opus subagents implement (D-041).
- 2026-08-25 — D-058: User approved SC §14.1 budget revisions with the PRD-003 build plan: preparation 2,000 → 3,000; production module cap 50 → 68; tests 8,000 → 9,000; declarative 3,000 → 3,500 (scope label now names evaluation catalogs). Production total 15,000 and grand total 26,000 unchanged. `tools/budget_check.py` constants updated together; wave projection: preparation ≈ 2,840, modules 66/68, tests ≈ 8,810, declarative ≈ 3,378. Set from measured PRD-002 reality per D-054, with margin so the wave avoids a T-013-style mid-task block.
- 2026-08-25 — D-057: PRD-003 Amendment 1 (V1-lite), user-approved with the build plan after explicit discussion of agent I/O styles: the PreparationAgent uses hydrated single-shot tasks (no runtime model tool loop; extends D-053 — the walls need every inspection/gap/impact report computed anyway, so hydration adds ~no code while a tool loop adds loop+surface+tests); artifact types consolidated 25 → 9 (StabilizationRecord, PreparationPlan, ExecutionReceiptBundle fold the §5.1 list; operation-level lineage with replay-recoverability replaces the per-cell ledger); walls 13 → 6 (same checks grouped); `source_row_id` = CSV hash + row number + canonical content hash (raw-byte scanner deferred). SC §5.4 PRD-003 row and §3.1 PreparedFrameBundle parents aligned. A bounded read-only pull loop requires a future user-approved amendment backed by pilot/eval evidence.
- 2026-08-25 — D-055: User approved the second design-budget revision ("approve 3400"): SC §14.1 design 2,750 → 3,400; PRD-002 criterion 39 and budget_check updated; hashes recomputed. graph.py (790) split structurally by phase into harness_base.py (state/deps/helpers, 340), harness_nodes.py (entry→method nodes, 277), graph.py (closing nodes + assembly + coordinator API, ~236) — all under the 350 module cap. Nothing was deleted; the split follows the harness's two-phase shape.
- 2026-08-25 — Environment note: the UF_HIDDEN/.pth issue recurred twice more this session; same fix. Graphviz 15.1.1 (the exact pin) is now installed locally via Homebrew.
- 2026-08-25 — D-052: Registry correction found by T-013 integration: TableSelection and DeliveryCapacityCheck lacked preparation-harness readers, which would make the §23 handoff gate refuse reader_not_allowed. Readers + preparation destinations added.
- 2026-08-25 — D-053: V1 model tasks receive fully harness-hydrated context sections; the Vertex gateway runs no tool-calling loop (AFC disabled per SC §10.4), so the ToolRouter and retrieval handlers are contract-tested but not model-driven at runtime in V1. PRD-002 §9.5 rule 3 (harness hydrates in batched requests, workers never fetch) is the governing reading. Revisit if a tool loop is added to the gateway.
- 2026-08-25 — D-054: T-013 implementation landed complete and green (650 tests; e2e approved design runs; durable interrupt/resume; real dot 15.1.1 — amends D-042: the pinned Graphviz version is now installed locally). Budget actuals: design 3,338/2,750; graph.py 790/350. The pre-code rethink was already consumed, so the breach is terminal per SC §14.1.2. Options for the user: approve design 2,750→3,400 plus a structural split of graph.py into ≤350-line modules (compaction to ~3,200 planned under that approval), or reduce PRD-002 scope. My projections under-estimated the harness twice (T-012 targets ~40% under; graph.py 2.8×) — recorded so the PRD-003/004 budget expectations get set from measured stage-harness reality, not optimism.
- 2026-08-25 — D-051: User explicitly approved the SC §14.1 budget revision design 2,500 → 2,750 ("yes I approve", 2026-08-25). SYSTEM-CONTRACT §14.1 row, PRD-002 acceptance criterion 39, and tools/budget_check.py updated together; governing-document hashes recomputed above. Production total stays 15,000 and binds on actual lines. T-013's blocked_complexity_budget is lifted.
- 2026-08-25 — D-049: T-013 preparation moves (commit 6ac88b9): ToolRouter/tool_allowlists → shared/toolrouter.py (identical enforcement for PRD-003/004/005 per SC §5.4); CatalogReader/ProductsReader/SqlCatalogReader + FrameSource/BytesFrameSource/CsvObjectFrameSource → shared/readers.py (PostgreSQL/object access is a shared responsibility; PRD-003 reads the same surfaces). Design 2,454 → 2,310.
- 2026-08-25 — D-050: PRD-002 V1 dispatches worker batches sequentially inside the ≤8 cap (§9.1 permits parallelism, does not require it); removes parallel-dispatch wiring from the T-013 plan. Recorded with the T-013 pre-code ComplexityRethinkV1, which still projects 2,685–2,705 against the 2,500 design ceiling → T-013 is blocked_complexity_budget before feature code per SC §14.1.2. The user decides: approve design 2,500→2,750 (one §14.1 cell; production total 15,000 unchanged and binding) or reduce PRD-002 scope (no cut exists inside the frozen PRD). No code was written for T-013 beyond the D-049 moves.
- 2026-08-25 — D-048: Common validation machinery moved to `src/causal/shared/validation.py` (issue/report/rule models, declarative-rule loader, strict payload parsing, evidence-class resolution, claim self-citation, shape wall, cycle check). SC §14.1 names "common validation" a shared responsibility; PRD-003/004 validators are its next consumers. This was T-012's consumed rethink after the design scope hit 2517/2500. Same pass: migration 0005 adds scope_id to the context_requirements key; askgate's answer-schema vocabulary extended to the registry ids (free-text.v1, column-list.v1, enum-choice.v1, duration-window.v1, level-map.v1, timing-class.v1, mapping-list.v1) so registry requirements are actually askable; askgate evidence-class table deduped onto shared.
- 2026-08-24 — D-047: T-011 resolutions. Batch sizing constant BATCH_COLUMN_LIMIT=4 with size max(4, ceil(n/8)) — pins the 8-batch cap for any column count. supported_estimands[0] is each pack's default (ITT for RCT); multiplicity policy encoded as an RCT invalidation rule. StructuralFieldV1.dtype is declared/undeclared (catalog views expose slot availability, not values) and ordinal is the catalog's deterministic per-table rank. Tool event ids are monotonic per envelope. The T-011 rethink was consumed converting make_retrieval_handlers (88 lines) into module-level handlers over a frozen RetrievalContext.
- 2026-08-24 — D-045: LangSmith 0.11.0 has no per-batch ingest acknowledgement. The flush gate's verifiable contract is: client constructed with tracing_error_callback (fires once per exhausted ingest attempt) + post-flush tracing-queue-drain check; either signal raises ObservabilityError(flush_unacknowledged). Limits (sampled-out batches invisible; detection per flush, not per run) are recorded at the flush site. Span create/close failures map to flush_unacknowledged. SDK retries in google-genai default to 5 attempts; the gateway pins attempts=1 explicitly per SC §10.4.
- 2026-08-24 — D-044: T-009 Amendment 1. RoleName has 18 values (17 roles + unknown); an approved DesignOutcome requires all five refs (design, frame contract, graph view, capacity check, approval) non-None; the registry gains an eighteenth design row `DesignApprovalDecision` so the SC §11.1 verbatim CLI decision commit does not fail closed, and `DesignApproval` lists it as an optional parent. Registry now 26 rows.

## Checkpoint log

One line per checkpoint commit: date, commit subject, what state it freezes.

- 2026-08-24 — `4f7cbaf` T-001: environment (exact §14 pins, uv.lock), package skeleton, budget checker + 36 tests.
- 2026-08-24 — `bd6c985` T-002: canonical serialization + hashing + ArtifactEnvelopeV1/HandoffManifestV1 + 40 tests. Full suite 76 green.
- 2026-08-24 — Environment note: repo lives under ~/Documents (likely iCloud-synced); macOS set UF_HIDDEN on venv files, which makes Python 3.12 silently skip .pth files. Cleared with `chflags nohidden`; if imports break again after re-sync, re-run: `find .venv -flags +hidden -exec chflags nohidden {} +`
- 2026-08-24 — `7b96eeb` T-003: OperationalEventV1 + emitter, 21 tests.
- 2026-08-24 — `99d0132` T-004: artifact-type registry + seeded rows, 11 tests. Full suite 107 green; budget `within_budget` (shared 355, tests 769, declarative 35, 13 modules).
- 2026-08-24 — `c2fc732` T-005: persistence + commit protocol, 14 dockerized tests. Suite 121.
- 2026-08-24 — `ec5db6d` T-006: handoff store + gate, 11 dockerized tests. Suite 132.
- 2026-08-24 — `87629ec` T-007: intake core (contracts/archive/profiler + 6 registry rows), 58 tests. Suite 181 green; budget `within_budget` (shared 745, intake 344, tests 1570, declarative 166; 18 modules).
- 2026-08-24 — `faad0c8`/`a26c270` T-008 spec frozen + two pre-implementation amendments (D-027..D-038).
- 2026-08-24 — `e613eed` T-008: Kaggle capture Protocol, field-class registry, evidence/semantic-map builders, catalog migration + 5 views, coordinator with idempotency/refusals/handoff opening, shared build_event/build_envelope. 34 tests; suite 215 green; budget `warning` (intake 1180/1200, shared 792/2500, tests 2050/8000, declarative 291/3000, largest module 329/350; 25 modules). Intake budget is nearly exhausted by design — PRD-001 is complete; nothing further lands in the intake scope except fixes.
- 2026-08-24 — Environment note: the UF_HIDDEN/.pth issue recurred mid-session and was cleared again with `find .venv -flags +hidden -exec chflags nohidden {} +`. MinIO fixture now retries on XMinioServerNotInitialized, not just connection refusal.
- 2026-08-24 — `2c98730` T-009: shared AgentTaskEnvelopeV1/AgentTaskResultV1/ContextRequirementV1/ClaimV1; design contracts/semantics/frame models; 18 design registry rows (26 total); 0004_design.sql (8 tables). 134 new tests; suite 349 green.
- 2026-08-24 — `ff4e7f8` T-010: gateway.py (250) + tracing.py (187) + committer flush gate (+6); 44 tests; suite 390 green + 2 gated live smokes. Vertex/LangSmith APIs introspected from installed pins, none guessed.
- 2026-08-24 — `d6d4cf2` T-011: packs.py/triage.py/entry.py/tools.py + method-packs/context-requirements/design-tools registries + ColumnTriageRecord row (27 types); 67 tests; suite 457 green.
- 2026-08-25 — `8ec3e94` T-012: validators (344→walls-only) + askgate + diagnostics + capacity + 3 registries + shared/validation.py + 0005 migration; 139 tests; suite 596 green.
- 2026-08-25 — `6ac88b9` T-013 prep: shared toolrouter + readers; design 2310/2500, shared 1732/2500; suite 596 green.
- 2026-08-25 — T-013 spec + rethink frozen; task BLOCKED_COMPLEXITY_BUDGET awaiting user decision (D-050).
- 2026-08-25 — `84d83cf` T-013: LangGraph design harness (16 nodes, 3 durable interrupts, §16.4 corrections, §23 handoff), compile + prompts + renderer; D-055 budget revision + module split; 54 tests; suite 650 green.
- 2026-08-25 — `97e1a65` T-014: cli/main+render (410), runtime composition (349) + live Kaggle adapter (75); 67 tests; suite 717 green + 3 gated live smokes. PRD-002 wave complete: T-009..T-014 all ACCEPTED.
- 2026-08-25 — T-013 Amendment 6 (pilot fix): slot vocabulary in semantic prompt + issue detail in corrections; suite 737 green.
- 2026-08-25 — T-013 Amendment 5 (pilot fix): many-item response schema + hardened fan-out parse; shared +8; suite 735 green.
- 2026-08-25 — Round-4 pilot fixes (T-014 A4, T-008 A4, T-013 A4): flat metadata, question evidence, allowlist hydration + TaskRunner move; design 3,337, shared 1,863; suite 733 green.
- 2026-08-25 — T-008 Amendment 3 + T-013 Amendment 3 (pilot fixes): structural presence rows (intake 1,182) + prompt requirement vocabulary (declarative 2,115); suite 729 green.
- 2026-08-25 — T-013 Amendment 2 (pilot fix): real response schema; shared 1,755, design 3,398 unchanged; suite 727 green.
- 2026-08-25 — T-014 Amendment 3 (pilot fix): typed terminal results for harness-boundary errors; runtime 479/800; suite 722 green.
- 2026-08-25 — T-010 Amendment 1 (pilot fix): 31-bit Vertex seed; suite green; SC hash recomputed at commit.
- 2026-08-25 — T-014 Amendment 2 (pilot fix): live version resolution via dataset_list; runtime 427, tests 6,354; suite 719 green.
- 2026-08-25 — Step 1 of the PRD-003 wave (D-057..D-059): PRD-003 Amendment 1 (V1-lite) appended; SC §5.4/§3.1/§14.1 aligned and budgets revised; budget_check constants updated; hashes recomputed. Checker `warning` (pre-existing near-cap scopes only), no breaches.
