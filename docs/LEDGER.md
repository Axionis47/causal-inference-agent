# Project Ledger

Single source of truth for project state. Any session restores full context by
reading `docs/product/SYSTEM-CONTRACT.md`, the five PRDs, and this file.
Fable owns this file; Opus reads it and verifies hashes from it.

Statuses: `DRAFT` → `READY_FOR_OPUS` → `IN_PROGRESS` → `REVIEW` →
`ACCEPTED` | `REVISION_REQUIRED` | `FABLE_REVISION_REQUIRED`

## Governing document hashes

Recomputed and updated by Fable at every doc freeze
(`shasum -a 256 docs/product/*.md`).

| Document | SHA-256 | Frozen at (commit) |
|---|---|---|
| SYSTEM-CONTRACT.md | `fa9071bfe60af1e113659fee82dc7ad36ec64fc2fb5b964eeffcb3ca2b475ee4` | D-100 amendment (§5.4 tool allowlists emptied), 2026-08-30 |
| PRD-001-kaggle-intake-storage.md | `a69337d3bfeac33cc7c017da357df369e01fe2c00d2b8ade429b84eac6913188` | initial freeze |
| PRD-002-causal-design-harness.md | `59281bee16c28c441e2970e4fc4604721a888372a1c1995ebe67510135e2d8cc` | D-100 amendment (§15 model-facing tools deleted), 2026-08-30 |
| PRD-003-runnable-frame-preparation.md | `e2fa601f633672424e2f683b96ad510f53d82363e8513e7b77a1739609a0ef78` | Amendment 2 (V1-mech, D-076), 2026-08-26 |
| PRD-004-estimation-diagnostics-judgment.md | `895e49331bdaceb5978604971a42bb6ac6937c8f47f6b505cfbdf794ea4de38e` | Amendment 1 (V1-mech, D-083), 2026-08-26 |
| PRD-005-evidence-visualization-presentation.md | `6f00087cd29cfd052d61b305a3f2fe22333c25cef22d8b2c669b243aad5f5f37` | initial freeze |

## Task board

| Task ID | Title | Owning PRD | Depends on | Model | Status | Doc hashes verified | Notes |
|---|---|---|---|---|---|---|---|
| T-001 | Repo scaffolding, uv lock, budget checker | shared (SC §14, §14.1) | — | single-session (D-008) | ACCEPTED | yes | commit `4f7cbaf`; 154 pkgs resolved; 36 tests; budget `within_budget` |
| T-002 | Shared contract kernel (canonical, hashing, envelopes) | shared (SC §2, §3, §8.2) | T-001 | single-session (D-008) | ACCEPTED | yes | commit `bd6c985`; canonical.py adopted from causal-final-de draft with D-006 fix; 40 tests; maps to EV-SYS-001; budget `within_budget` (shared 158, tests 576) |
| T-003 | OperationalEventV1 + NDJSON emitter | shared (SC §10.1) | T-002 | single-session (D-008) | ACCEPTED | yes | commit `7b96eeb`; 28 event names; 21 tests; maps to EV-SYS-005 |
| T-004 | Artifact-type registry + seeded registrations | shared (SC §3.1) | T-002 | single-session (D-008) | ACCEPTED | yes | commit `99d0132`; registry fail-closed on unsupported_schema; 2 rows seeded (D-012); 11 tests; maps to EV-SYS-001/EV-SYS-006 |
| T-005 | Persistence + §8.2 commit protocol | shared (SC §3, §4, §8) | T-002, T-003, T-004 | single-session (D-008) | ACCEPTED | yes | commit `c2fc732`; 14 dockerized integration tests (postgres:18.6 + MinIO); trace-flush gate deferred (D-020); maps to EV-SYS-001/EV-SYS-004 |
| T-006 | Handoff persistence + acceptance gate | shared (SC §3, §3.1, §8.1) | T-005 | single-session (D-008) | ACCEPTED | yes | commit `ec5db6d`; fail-closed, all codes collected; 11 dockerized tests; maps to EV-SYS-006 |
| T-007 | Intake core: contracts, registrations, archive, profiler | PRD-001 (§5.5–§8) | T-004 | single-session (D-008) | ACCEPTED | yes | commit (see checkpoint log); 6 registry rows appended; 58 tests; maps to EV-P1-003/EV-P1-004 |
| T-008 | Intake capture + catalog + coordinator + handoff | PRD-001 (§4, §9–§13) | T-005, T-006, T-007 | single-session (D-008) | ACCEPTED | yes | commit `e613eed`; spec + amendments D-027..D-038; 34 tests; maps to EV-P1-001/002/005; budget `warning` (intake 1180/1200, coordinator 329/350 — both inside limits, near them) |
| T-009 | Design substrate: shared task envelope, design contracts, registry rows, design schema | PRD-002 (SC §5.2, §6.1, §21; §6, §12, §16–§18) | T-002, T-004, T-005, T-008 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit `2c98730`; spec + Amendment 1 (D-044); 134 new tests, suite 349 green; budget `warning` only on pre-existing intake dims (design 601/2500, shared 982/2500, tests 2678/8000, declarative 641/3000, 28 modules) |
| T-010 | Shared Vertex gateway + LangSmith tracing | shared (SC §10.2–§10.4) | T-009 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 44 new tests + 2 env-gated live smokes; closes the D-020 revisit (flush gate live in ArtifactCommitter, D-045); maps to EV-SYS-003/EV-SYS-005; shared 1425/2500 |
| T-011 | Method-pack manifests, triage, DesignContextManifest, tool surface, entry gate | PRD-002 (§4, §5, §9, §13, §15) | T-009 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 67 new tests, suite 457 green; rethink consumed (make_retrieval_handlers 88→module-level handlers); maps to EV-P2-001/003, EV-SYS-002; design 1492/2500, declarative 1324/3000 |
| T-012 | Validation walls, ask gate, diagnostics, delivery capacity | PRD-002 (§10, §11, §14, §16.3, §13.5) | T-011 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 139 new tests, suite 596 green; rethink consumed (2517→2454 via D-048 move); maps to EV-P2-004/005/006/007, EV-SYS-002; design 2454/2500 — T-013 must budget via further consolidation |
| T-013 | Agents, prompts, LangGraph graph, interrupts, renderer, coordinator, outcome + handoff | PRD-002 (§8, §9, §12.3, §19–§23) | T-010, T-012 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 54 new tests, suite 650 green incl. e2e approved runs + durable resume; unblocked by D-055 (design 3,400); graph split into harness_base/harness_nodes/graph (340/277/~236); maps to EV-P2-001..008; design 3,398/3,400 |
| T-015 | Preparation substrate: contracts, plans, pack overlay, receipts, 9 registry rows, migration 0006 | PRD-003 §24 (V1-lite) | T-009..T-014 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 152 new tests, suite 891 green; preparation 593/3,000, shared 1,973/2,500, declarative 2,753/3,500, tests 7,361/9,000; registry 36 rows; deviations D-073 |
| T-018 | Entry gate, gap triage + plan compiler, six walls | PRD-003 §4/§7/§14(§24.3)/§16 | T-015..T-017 | opus subagent (D-041) | ACCEPTED | yes | commit (see checkpoint log); 50 tests, suite 1,047 green; preparation 2,472/3,000, declarative 2,960/3,500, tests 8,600/9,000, modules 65/68; rethink consumed (density pass); deviations D-075 |
| T-017 | Phase B: operations, sequential executor, diagnostics | PRD-003 §10/§11/§15/§17 (§24) | T-015 | opus subagent, isolated worktree (D-073) | ACCEPTED | yes | merged `f7ddf0a`; 58 tests; rethink consumed (consolidation); preparation 1,264→ merged 1,753/3,000; deviations D-074 |
| T-016 | Phase A: row identity, stabilization engine, impact, shared frames | PRD-003 §8/§9/§12 (§24) | T-015 | opus subagent, isolated worktree (D-073) | ACCEPTED | yes | merged `3772ea6`; 50 tests incl. hypothesis + live MinIO round-trip; rethink consumed (deletion/consolidation, stabilize 382→298); deviations D-074 |
| T-020 | Evaluation catalog: evals/catalog.v1.yaml | shared (SC §10.5) + all PRD eval tables | T-019 | opus subagent (D-076) | ACCEPTED | yes | commit (see checkpoint log); 34 registrations (SYS 7, E2E 7, P1 5, P2 8, P3 7) byte-verbatim vs tables; 6 tests (71 logical); declarative 3,329/3,500, tests 8,949/9,000; deviations D-079 |
| T-019 | Deterministic preparation coordinator, runtime dispatch, PRD-004 handoff | PRD-003 §7/§17.7/§20/§25 (V1-mech, D-076) | T-015..T-018 | opus subagents, sliced (D-076) | ACCEPTED | yes | commit (see checkpoint log); 13 new tests (309 logical); suite 1,058 green; preparation 2,995/3,000, runtime 597/800, cli 423/500, shared 2,095/2,500, tests 8,878/9,000, modules 66/68; deviations + open design-ordering finding D-077 |
| T-022 | Estimation substrate: contracts, packs, registries | PRD-004 §5/§6/§8/§26 | T-002..T-006, T-019 | opus subagent (D-083) | ACCEPTED | yes | 126 tests, suite 1,191 green; estimation 460/4,000, declarative 4,001/4,600 (watch), tests 9,331/13,000, modules 68/86; deviations D-084 |
| T-023 | Estimation plan spine: entry, plan compiler, capacity recheck, 15 walls | PRD-004 §4/§6.1/§6.5/§18/§26 | T-022 | opus subagent (D-083) | ACCEPTED | yes | 137 new tests, suite 1,244 green; estimation 950/4,000, declarative 4,087/4,600, tests 9,668/13,000, modules 70/86; deviations D-085 |
| T-024 | Estimation coordinator substrate: harness, engine, 0007 migration | PRD-004 §6.2/§6.4/§14/§15/§16.1/§19/§21 (§26) | T-022, T-023 | opus subagent (D-083) | ACCEPTED | yes | 54 new tests, suite 1,298 green; estimation 1,440/4,000, declarative 4,138/4,600, tests 10,154/13,000, modules 72/86; deviations + producer ruling D-086 |
| T-025 | Claim judgment: contracts, bounded claim-review call, deterministic validator | PRD-004 §16/§20.3 (§26) | T-022, T-024 | opus subagent (D-083) | ACCEPTED | yes | 15 new tests, suite 1,313 green; estimation 1,690/4,000, declarative 4,221/4,600, tests 10,396/13,000, modules 73/86; deviations D-087 |
| T-026 | RCT adapter + coordinator nodes: first e2e `complete` bundle | PRD-004 §7/§9/§17/§22 (§26) | T-023..T-025 | opus subagent (D-083) | ACCEPTED | yes | 11 e2e + support tests, suite 1,324 green; estimation 2,351/4,000, shared 2,097/2,500, tests 10,800/13,000, modules 75/86; deviations D-088 |
| T-027 | AIPW adapter: cross-fit, nuisance profiles, score, overlap diagnostics | PRD-004 §10/§17 (§26) | T-026 | opus subagent, isolated worktree (D-089) | ACCEPTED | yes | 12 cases; merged suite 1,357 green; aipw.py 345; deviations D-089 |
| T-028 | DiD (simultaneous + Sun–Abraham) and sharp-RDD adapters | PRD-004 §11/§12/§17 (§26) | T-026 | opus subagent, isolated worktree (D-089) | ACCEPTED | yes | 21 tests; merged suite 1,357 green; did 303 / rdd 238; open items → T-029; deviations D-089 |
| T-029 | Estimation runtime dispatch, CLI render, EV-P4 catalog, D-089 items | PRD-004 §20.6/§24#49; SC §1.1 | T-026..T-028 | opus subagent (D-083) | ACCEPTED | yes | +14 tests, suite 1,371 green; runtime 702/800, cli 435/500, estimation 3,279/4,000, declarative 4,304/4,600, tests 11,584/13,000; deviations + open chain item D-090 |
| T-030 | Presentation substrate: contracts, visualization catalog, entry gate | PRD-005 §5/§7.1/§8/§10/§18 | T-026, T-029 | opus subagent (D-083) | ACCEPTED | yes | 56 tests, suite 1,427 green; presentation 404/1,500, declarative 4,511/4,600 (hard watch), tests 11,900/13,000, modules 80/86; font vendored + sha verified; deviations D-092 |
| T-031 | Presentation pipeline: curator, plan validator, compiler, renderer | PRD-005 §9/§10/§11/§13/§14 | T-030 | opus subagent (D-083) | ACCEPTED | yes | 35 new tests, suite 1,462 green; presentation 941/1,500, declarative 4,554/4,600, tests 12,307/13,000, modules 83/86; deviations D-093 |
| T-032 | Presentation coordinator, five gates, bundle, CLI delivery, EV-P5 | PRD-005 §6/§15/§16/§17.2/§18/§19.1/§20 | T-031 | opus subagent (D-083) | ACCEPTED | yes | 12 new tests, suite 1,473 green; presentation 1,372/1,500, production 15,488/15,500, modules 86/86; deviations D-095 |
| T-033 | AIPW/DiD presentation-contract alignment | PRD-004 §17; PRD-005 §5/§11.4/§12/§16 | T-029, T-032 | single-session | ACCEPTED | yes | AIPW `complete_with_qualifications`; DiD `complete`; four-pack producer closure; suite 1,489 green; D-107..D-109 |
| T-034 | Real-data sensitivity terminal correction | PRD-004 §15/§18/§26 | T-024, T-028, T-033 | single-session | ACCEPTED | yes | Actual Groupon AIPW and state-panel DiD deliver with qualifications; suite 1,490 green; D-110..D-111 |
| T-014 | CLI seven commands, runtime composition, live Kaggle adapter | shared (SC §1.1) + PRD-002 §11.1 | T-013 | single-session + opus subagents (D-041) | ACCEPTED | yes | commit (see checkpoint log); 67 new tests, suite 717 green; closes D-034; maps to EV-SYS-007; cli 410/500, runtime 421/800; modules 50/50 and largest module 350/350 — both caps fully consumed |

## Decision log

Append-only. One line per decision: date, decision, why.

- 2026-09-03 — D-111: T-034 accepted. Actual-data reruns over Groupon deals (710 rows) and the 2005–2011 state minimum-wage/poverty panel (357 rows, 51 states) both reached PRD-003 `prepared`, PRD-004 `complete`, and PRD-005 `complete_with_qualifications`. AIPW estimated +156.731 quantity sold (95% interval 40.505–272.956) after price adjustment; weighted price balance remained above the 0.1 threshold, so the claim stayed qualified. The simultaneous DiD federal-exposure design estimated −0.0328 poverty-rate points (−0.4295–0.3639); the hand-computed two-by-two result matched, but the pre-period test rejected parallel trends (p=0.00055), so no credible causal effect is concluded. One inapplicable not-yet-treated branch stayed visibly failed and qualified without blocking delivery. Wall 10 now checks planned-branch presence rather than rejecting the typed `failed` terminal status required by PRD-004 §15. Focused tests passed; full suite 1,490 passed / 3 skipped; ruff and mypy clean; budget has no breaches (estimation −1, tests +16).
- 2026-09-02 — D-110: T-034 frozen after the actual state-panel DiD run computed the primary estimate and all diagnostics but stopped at wall 10. PRD-004 §15 and §26 explicitly define a visible typed failed sensitivity as terminal; `run_sensitivities` follows that contract, while `_sensitivities_terminal` incorrectly equated `failed` with an omitted branch. RULING: wall 10 checks presence of every planned branch and accepts all schema-valid execution statuses as terminal. The failed branch remains visible, qualified, and non-computed; no statistical calculation changes. The Groupon `unknown_rule_target` is separately classified as a run-fixture key-name mismatch, not a product defect.
- 2026-09-02 — D-109: T-033 accepted. AIPW and DiD now pass the real PRD-003 → PRD-004 → PRD-005 journey: AIPW delivers `complete_with_qualifications` with all frozen qualification IDs beside the primary estimate and in its caption (five required figures); DiD delivers `complete` with trends, event-time, cohort-by-event-time grouped support counts, and the primary aggregate (four required figures). The new four-pack closure test binds design-required evidence = presentation-required evidence ⊆ registered estimator-produced evidence, so the original contract drift fails immediately. Focused presentation/estimation suite 108 passed; full suite 1,489 passed / 3 skipped with only the existing pyfixest/runtime warnings; ruff and mypy clean. Budget report `warning` only for existing near-cap cells and has no breaches: estimation 3,287/4,000 (+1), presentation 1,371/1,500 (+2), tests 12,577/13,000 (+56), declarative 4,594/4,600 (−2), production 15,323/15,500, modules 84/86. No unrelated untracked file was changed.
- 2026-09-02 — D-108: T-033 Amendment 1 frozen after the registry correction let DiD reach PRD-005 gate 4. AIPW delivered `complete_with_qualifications`; DiD rendered trends and event-time evidence, then its support/composition figure measured 736×1,332 and correctly stopped `needs_layout_revision`. The frozen support points already carry cohort in `category` and relative event time in `x_value`, but the builder overwrites their series with the constant `support`; `composition_stack.v1` maps category to x and series to group, so ten period rows stack at each cohort. The compiler comment says grouped bars separate groups by position, but its encoding adds color only and no x-offset. RULING: make the DiD support builder expose cohort as series and event time as category, and add the missing nominal grouped-bar x-offset in the compiler. This changes only how frozen counts are arranged; no statistical value, domain, or evidence requirement changes. T-033 production allowance becomes ≤8 lines, within existing headroom.
- 2026-09-02 — D-107: T-033 frozen with user approval after a real four-method preparation → estimation → presentation run. Randomized experiment and sharp RDD delivered; AIPW estimated 2.002 (95% interval 1.846–2.159) and DiD estimated 2.008 (1.989–2.026), then both stopped at PRD-005 gate 1 with `missing_figure_data`. ROOT CAUSE: `method-packs.v1.json` and the AIPW/DiD visualization profiles classify `qualifications` as required visual evidence, while their registered adapters produce every statistical family in PRD-004 §17 but correctly produce no qualifications FigureData. PRD-005 §12 likewise omits qualifications from the visual questions and §11.4 separately requires each actual ClaimJudgment qualification beside the primary result and in its caption. RULING: remove `qualifications` from both method packs' required visual evidence and move it to conditional evidence in both presentation profiles; do not invent a numeric qualifications artifact and do not weaken the existing placement validator. This is an in-place pre-release V1 correction; affected local analyses must rerun. Add one four-pack producer-closure contract test and AIPW/DiD presentation e2e regressions; no production code or budget-ceiling change.
- 2026-08-24 — Repository initialized with two-terminal Fable/Opus write-ownership enforcement (hooks in `.claude/`). Rationale: mechanical enforcement of the role contract instead of honor-system.
- 2026-08-24 — D-001: Task specs live as one file per task under `docs/tasks/`, referenced from PRDs/contract by section; keeps frozen PRDs stable and hashes per-task.
- 2026-08-24 — D-002: Wave 1 = T-001 (scaffolding) ∥ T-002 (contract kernel); disjoint write sets, T-002 depends on T-001 only for the runnable environment.
- 2026-08-24 — D-003: The §14.1.1 budget checker lives at `tools/budget_check.py`, standard library only, assigned to the tests scope for counting (verification tooling, not production behavior). Open to revision if Opus shows a contradiction.
- 2026-08-24 — D-004: Canonical JSON = UTF-8, code-point-sorted keys, `(",", ":")` separators, NFC strings, no NaN/Inf, None preserved, RFC 3339 UTC timestamps with exactly 6 fractional digits and `Z`; hashes are bare 64-char lowercase hex.
- 2026-08-24 — D-005: Identity fields are non-empty strings ≤200 chars at the envelope layer; stricter per-identity formats are enforced at creation sites, not in shared contracts.
- 2026-08-24 — D-006: Canonicalization raises stable code `duplicate_key_after_normalization` when two mapping keys collide under NFC; silent collapse would break key-order determinism. (Raised by Opus during T-002 drafting.)
- 2026-08-24 — D-007: `payload_locator` is exempt from the 200-char identity cap; its own cap is 1024 chars. (Raised by Opus during T-002 drafting.)
- 2026-08-24 — Enforcement finding: write-guard hooks verified live in fresh sessions (headless probe denied correctly) but inert in sessions that predate the settings file or have not approved project hooks; every interactive session must run /hooks once (or restart and accept the hook prompt) and then verify behaviorally.
- 2026-08-24 — D-008: User dissolved the two-terminal Fable/Opus model. Single session architects and implements; hooks removed from settings (scripts kept inert in .claude/hooks/); ledger/checkpoint discipline retained. T-001/T-002 reassigned to the main session. Budget-checker path exclusions fixed as: docs/**, .claude/**, CLAUDE.md, .gitignore, .python-version, pyproject.toml, uv.lock, README.md.
- 2026-08-24 — D-009: `registries/` is a top-level declarative-scope directory for static registries.
- 2026-08-24 — D-010: Static registries are JSON, not YAML (stdlib-parseable; the pinned stack has no YAML parser and none is added).
- 2026-08-24 — D-011: Canonical component IDs: intake-coordinator, design-harness, preparation-harness, estimation-harness, presentation-coordinator, cli, runtime.
- 2026-08-24 — D-012: artifact-types registry file is append-only; each stage task appends its rows once the owning PRD is read, so no parent-type name is invented ahead of its PRD.
- 2026-08-24 — D-013: `Stage` enum includes `system` for shared/CLI/runtime components acting outside one stage.
- 2026-08-24 — D-014: OperationalEventV1 `versions` keys are the closed set {model, prompt, tool, registry, schema, validator, compiler, renderer}.
- 2026-08-24 — D-015: `token_usage` keys are the closed set {input, output, thinking, total}.
- 2026-08-24 — D-016: The emitter (not the model) enforces name registration and requires non-empty `required_eval_ids` on task./agent./tool./handoff. events.
- 2026-08-24 — D-017: Dev/test object store is MinIO via Docker; src/ depends only on an S3 client Protocol (no boto3 import in production code). Postgres runs as the pinned postgres:18.6 image.
- 2026-08-24 — D-018: Legal run-state transition map fixed from the §4 diagram; completed/failed/failed_observability are terminal at the stage-run level.
- 2026-08-24 — D-019: payload_locator format is `objects/{content_hash}`; the committer rejects disagreement.
- 2026-08-24 — D-020: §8.2's LangSmith flush gate is deferred to the tracing task; commit currently ends at reopen-validation + artifact.committed event. Deliberate, recorded, must be revisited when LangSmith lands.
- 2026-08-24 — D-021: The handoff receiver names its expected outcomes explicitly per seam; "wrong outcome" is seam knowledge, not manifest knowledge.
- 2026-08-24 — D-022: Handoff gate events are caller-built via an event factory so the D-016 eval-ID rule stays satisfiable.
- 2026-08-24 — D-023: Intake-internal registrations use terminal status `committed`; outcome statuses live only on IntakeOutcome. KaggleCapture is restricted, readable only by intake-coordinator.
- 2026-08-24 — D-024: kaggle_ref normalizes to owner/slug; owner/slug or kaggle.com/datasets URL accepted.
- 2026-08-24 — D-025: Profiler hypotheses limited to two bounded deterministic rules (identifier, ±9…±9999 sentinel at ≥1% and min/max), always labelled hypothesis.
- 2026-08-24 — D-026: Archive safety limits are constructor parameters (10k entries, 512 MiB file, 2 GiB total, 200:1 ratio above 1 MiB).
- 2026-08-24 — D-027: IntakeOutcome required parents reduced to [QuestionRecord]; the full chain moves to optional and the coordinator enforces it for usable/partial. An early refusal has no chain to cite; the registry must stay committable fail-closed.
- 2026-08-24 — D-028: Raw bytes (archive, extracted source files) are content-addressed objects without envelopes; catalog.resources rows point at them. Only canonical-JSON payloads get artifact envelopes.
- 2026-08-24 — D-029: PRD-001's `artifacts` table is realized by shared `causal.artifacts`; the `catalog` schema owns runs, datasets, resources, source_field_index, and exactly five narrow views. Run operational state stays in `causal.stage_runs`.
- 2026-08-24 — D-030: Provider field classification is the static registry `registries/kaggle-field-classes.v1.json`; unlisted fields default to `operational` (fail-closed away from model context).
- 2026-08-24 — D-031: Deterministic identities: analysis_id = an-sha16(idempotency_key); dataset_id = kaggle:owner/slug@version; artifact_id = kind:analysis_id:hash16. Payloads carry no wall-clock; envelope timestamps come from an injected clock.
- 2026-08-24 — D-032: v0 semantic-map rules are conservative: column meaning from provider description, missing_sentinel from profiler hypotheses, everything else not_offered. Free-text-to-slot interpretation belongs to PRD-002.
- 2026-08-24 — D-033: Outcome rule — refused per PRD-001 §12; partial when any resource is unreadable/excluded/failed or no semantic field is evidenced; usable otherwise.
- 2026-08-24 — D-034: The live kaggle==2.2.4 adapter is deferred to the CLI/runtime task; T-008 ships the client Protocol and capture layer; tests use frozen fixture clients. Credentials exist only at adapter construction from the runtime secret source.
- 2026-08-24 — D-035: Re-running an incomplete analysis uses a new stage_run_id; deterministic artifact IDs make recommits §8.2 replay no-ops (PRD-001 §13 restart-from-boundary).
- 2026-08-24 — D-036: resources.parse_status is PRD-001's five values (parsed, excluded, unreadable, unsafe, failed); withheld classifications map to excluded with a reason.
- 2026-08-24 — D-037: The producer never persists the handoff manifest (the T-006 gate records at receipt). The coordinator's open_handoff rebuilds it deterministically from analysis_id + intake_outcome_artifact_id and refuses for refused/missing/mismatched outcomes. Measured-fact index rows point at TableProfile artifacts. (T-008 Amendment 1.)
- 2026-08-24 — D-038: Shared docker fixtures live in tests/conftest.py so all test packages reuse them. (T-008 Amendment 2.)
- 2026-08-24 — D-039: PRD-002 wave plan = T-009 (substrate) → T-010 (gateway+tracing, shared) → T-011 (manifests/triage/manifest-compiler/tools/entry gate) → T-012 (validators/ask gate/diagnostics/capacity) → T-013 (agents/prompts/LangGraph/renderer/coordinator/outcome/handoff) → T-014 (CLI/runtime/live Kaggle). Specs freeze one task at a time, immediately before that task's implementation, so later specs absorb earlier findings. Declarative-first rule: method packs, requirement/tool/capacity registries, and prompt templates land in the declarative scope, keeping `design` code within its 2,500 lines.
- 2026-08-24 — D-040: AgentTaskEnvelopeV1, AgentTaskResultV1, ContextRequirementV1, and ClaimV1 live in the shared scope (`src/causal/shared/envelope.py`): SC §5.2/§5.4/§6.1/§16.2 define them across PRD-002–005.
- 2026-08-24 — D-041: Build mode for the PRD-002 wave (user-directed): Opus subagents implement the frozen specs; the main session architects, reviews, runs gates, and commits. The D-008 single-session discipline is retained at the spec/review/commit boundary.
- 2026-08-24 — D-042: The dev machine has no Graphviz binary; the Graphviz 15.1.1 pin binds the future OCI image. The renderer must record the actual local Graphviz version in every output and its tests must skip (not fake) when `dot` is absent. No silent substitution.
- 2026-08-24 — D-043: Session environment check: Kaggle credentials (`~/.kaggle/kaggle.json`) and Google ADC are present; no LangSmith key is visible. Tracing reads `LANGSMITH_API_KEY` from the environment at runtime and the preflight fails closed to `failed_observability` when it is missing, per SC §10.2.
- 2026-08-25 — D-056: T-014 seams. `causal approve-design` gains an optional `--changes-file PATH` (JSON list of strings), required exactly for `changes_requested` — the SC §1.1 flag table gives no carrier for the §22 change texts and the typed schema requires them; recorded as a deliberate one-flag extension. StatusView unified on the CLI's model (runtime imports it). The runtime validates interrupt id/hash/revision against the live checkpointed interrupt payload before consuming an idempotency key; approve fills approved_artifacts from the interrupt's own refs so the §22 binding cannot disagree with the revision under review. Kaggle adapter strips provider exception bodies (they can embed tokens) keeping only the class name. Module count 50/50 and largest module 350/350: both caps are fully consumed — PRD-003 needs either headroom recovered or a cap decision before its first module.
- 2026-08-31 — D-105: A table can now say "the row is the unit", closing the B1 blocker from D-104. `randomized_experiment`, `aipw` and `sharp_rdd` all require the `unit_identifier` role, and `lalonde_nsw (1).csv` has ten columns, all measurements, and `unique_single_columns: []`, so a correctly analysed randomised trial refused `UNSUPPORTED_METHOD`. 25 of its 445 rows are byte-identical to another row; as data, two 17-year-old men with ten years of schooling and no recorded earnings are indistinguishable from one man recorded twice, and no query can separate them. PRINCIPLE: this is an ASSERTION, never an inference. The trigger is three conditions checked together when the RoleLedger commits — no committed claim holds `unit_identifier`; the committed `TableProfile` measured `unique_single_columns == []`; and `DesignIntent.candidate_grain` states `one_row_per_unit`. The third carries the assertion, and it is load-bearing: "no column is unique" alone would turn a panel file that lost its id column into one independent unit per row and narrow every confidence interval. (1) `DesignIntentV1.candidate_grain` was `Identity` — a free string nothing validated and no prompt mentioned; the live run produced `one_row_per_unit` by luck of convention (fixtures held "one row per applicant", "person"). It is now a closed `TableGrain` Literal aligned with `design.table_grain`'s D-100 `choice:` schema and `packs.StructuralRequirement`, so `result_schema` renders it into the forced response schema and the bad value cannot be emitted; an unrecognised grain now fails at wall 1 with a correction instead of surfacing as `UNSUPPORTED_METHOD` five nodes later. (2) `harness_nodes._commit` binds the identifier into the RoleLedger (a `unit_identifier` claim on `__row_unit_id`, `status: hypothesis`, `support_class: corroborated_source_inference` — no document names a column that does not exist yet), the RunnableFrameContract (`key_columns`), and the ExperimentDesign (`assumptions`, so it is legible in the D-104 approval interrupt). This is harness-owned for the same reason `capacity_check` is (D-077): `key_columns` and `column_refs` are both in `validators._COLUMN_KEYS`, so wall 2 admits only `manifest.structural_inventory` names and no worker could name a column preparation has not created. RECORDED PLAINLY, NOT GLOSSED: the claim is appended AFTER wall 5 validated the payload, so it is not wall-checked, and its `concept_id` `c:row_unit` is not in the CausalContext's concept set. (3) `preparation/stabilize.with_row_unit` materialises the column from the source row order, called from `stabilize_node` — `harness.NODES` is `(entry, stabilize, plan, execute, outcome)`, so stabilization precedes every repair and an index taken later would shift under whatever those repairs dropped. `_identity_condition` then gets a column unique by construction and `estimation.harness.role_columns` resolves the role from the ledger with NO estimation change at all. REJECTED: per-estimator fallbacks — twelve call sites (`rct.py:142,154,186`, `aipw.py:399,484`, `did.py` ×9), and `rct.py:154` uses the unit column as the leave-one-out key, so a fallback that quietly stops clustering turns cluster-robust standard errors into plain ones with nothing raised. Also rejected: a sentinel in `key_columns`, which every consumer in preparation, estimation and the diagnostics would have to learn. USER RULING honoured: no deletion sweep funded this. Preparation landed at 2,997/3,000 and `preparation/nodes.py` at the 350-line module cap, reclaimed by joining one two-line `self.emit` call that fits on one line — a reformat, not a deletion. Budgets: design 3,237/3,400, shared 2,037/2,500, preparation 2,997/3,000, tests 12,503/13,000, modules 84/86, largest module 350/350, no breached dimension. Suite 1,472 passed / 3 skipped; ruff and mypy clean.
- 2026-08-31 — D-104: Audit of the two defect families, and the two Family A instances that remained. FAMILY A — the harness holds the fact and hands the model the label. Seven instances found across the session; five closed (D-098 `MeasurementLinkV1.timing` dropped by the compiler that computed it; D-098 the requirement vocabulary wall 2 enforced but no prompt rendered; D-102 the evidence bundle loaded and reduced to filenames; D-103 the `TableProfile` computed over the CSV and never shown; D-103 `detail: ""` on every validation issue). The two closed here: (A6) `harness_nodes.roles` sent `{batch_id, concept_ids, columns}` — the `ColumnSemanticCard`s hold the meaning, units, scale, levels and timing of those exact columns, were committed one node earlier and are already loaded by `measurement`, and PRD-002 §9.5's routing table says the role worker receives "approved intent, assigned concepts/relationships, timing, cited cards". The cards now go in the payload; this is a §9.5 conformance fix. (A7) `graph.approval` interrupted with artifact ids and a sha256 only, so the one point where a person gives informed consent to a causal design showed them `experimentdesign:an-…` and a hash. The committed design payload — its assumptions and identification risks, which D-103 made load-bearing — is now in the interrupt body. FAMILY B — the contract cannot express a legitimate, common case. (B1, OPEN, blocking) "the row is the unit" is inexpressible: `randomized_experiment`, `aipw` and `sharp_rdd` all require the `unit_identifier` role, and a plain cross-sectional CSV has `unique_single_columns: []`, so a correctly analysed randomised trial refuses `UNSUPPORTED_METHOD` (live: `an-f716b1a54102a897`, 445 rows, no unique column). Relaxing `required_roles` was REJECTED on evidence: estimation dereferences the column in twelve places (`rct.py:142,154,186`, `aipw.py:399,484`, `did.py` ×9 via `role_columns` in `estimation/harness.py:128`, which reads the RoleLedger claims), so dropping the requirement converts a clean refusal into a downstream `KeyError` and loses the guard separating 15 duplicate rows from 15 repeated measurements. Materialising a row index is ALSO larger than first scoped: `RunnableFrameContractV1.key_columns` is `min_length=1` and must be able to say "the row"; `preparation/stabilize._identity_condition` refuses when a key column is absent from the frame and `preparation/diagnostics` fails the identity check on duplicates; and `preparation/operations.registered_derivation` has no `row_index` branch and takes `target_arity: 1`. So B1 spans PRD-002 §18, PRD-003, and estimation, against 12 free preparation lines and 4 free declarative lines. It is NOT attempted here and is the next decision. (B2) `assignment_unit_uniqueness` returns `NOT_COMPUTABLE` with a warning when `key_columns` is empty — visible, not silent, and it closes with B1. (B3) STATED V1 LIMIT, not a defect: `RoleName.INSTRUMENT_CANDIDATE` exists with no instrumental-variables method pack, so a natural-experiment or IV design cannot be produced by the four-pack V1. Budgets: design 3,204/3,400, shared 2,035/2,500, preparation 2,988/3,000, declarative 4,596/4,600, tests 12,454/13,000, modules 84/86, no breached dimension. Suite 1,462 passed / 3 skipped; ruff and mypy clean.
- 2026-08-31 — D-103: The design stage reads what is available, decides, and records what it assumed. Four changes, answering the user's question "the context could be too much and too little, how are we handling all those". (1) TOO LITTLE — the registry was the policy file and it said "ask a human about everything": SIXTEEN of seventeen requirements were `blocking` + `ask_user`, so any absent Kaggle metadata became an interrogation instead of a design. `ExperimentDesignV1.assumptions`, `.identification_risks` and `.sensitivity_requirements`, `validators.RESOLVED_STATES = ("resolved", "unknown_accepted")`, wall 6's `retain_as_sensitivity` exemption and `gate()`'s `RECORD_SENSITIVITY` route were all built, wired end to end, and had NEVER been exercised. Re-tiered to FIVE blocking on one principle — blocking iff a wrong answer makes the ESTIMATE wrong: `design.causal_question` (no question, no design), `design.treatment_meaning` (decides what is estimated), `design.assignment_mechanism` (picks the method), `column.measurement_timing` (a post-treatment covariate is a collider), `design.table_grain` (decides independence and the standard errors). The other twelve route to sensitivity and land in the design as stated assumptions. Declarative, zero production lines. (2) TOO MUCH — `agenttask.evidence_block` gains `in_task_scope`, so a column-scoped evidence id reaches only a task assigned that column, plus `EVIDENCE_TOTAL = 24000` per prompt on top of `EVIDENCE_CHARS = 4000` per item, and an item dropped for budget renders `(withheld: this task's evidence budget is full)` rather than vanishing. (3) THE DATA ITSELF — the committed `TableProfile` (dtype, cardinality, null counts, numeric min/max/mean/quantiles, hypotheses) was computed over the real CSV and never shown; the semantic worker was asked "what does this column measure, in which units, on which scale, with which levels" and handed the column's NAME. `_evidence` now also yields `<profile>#/columns/<column>` ids whose text is the bounded fact set, which `evidence_class` already maps to `measured_observation`, so a card slot citing one is a measurement rather than an inference; table-level counts (`row_count`, `duplicate_row_count`, `unique_single_columns`) go to the intent task where grain and unit identity are decided. `triage_node`'s inline profile read is deduped onto the new `_profile`. (4) THE CORRECTION LOOP COULD NOT TEACH — TWO evidence-class tables disagreed: `askgate.EVIDENCE_CLASS_PREFIXES` called `ev:kaggle/` a `data_dictionary` while `shared/validation` called `ev:kaggle/dataset/` a `source_statement`, and `column.measurement_timing` accepts the first but not the second. That mismatch is what rejected the role worker's CORRECT citation of "re78 … (после вмешательства)" as `blocking_claim_unsupported` and burned the D-102 correction budget. askgate's table and `evidence_class_for` are deleted onto the shared one. `validation.unresolved_issues` also carried `detail: ""` on every issue — a worker got a code and a path and two attempts to guess what the harness would accept; it now names the offending value and the legal set. The prompts gained the line that stops the actual error: the model was writing the REGISTERED id into `fact_required` and minting `req-treat-timing` for `requirement_id`, reading the key as its own handle. LIVE RESULT on `an-f716b1a54102a897`: TEN `ColumnSemanticCard`s (2 before), THREE `RoleEvidence`, and `CausalContext` + `RoleLedger` COMMITTED FOR THE FIRST TIME. The ledger is correct causal work — treatment `treat` evidenced/concurrent, outcome `re78` evidenced/post_treatment, six confounder candidates (age, educ+nodegree, black+hisp, marr, re74, re75) all pre_treatment. Requirement rows fell to 15: six RESOLVED from the document with no question, seven recorded as sensitivities, and the ask gate put exactly TWO questions — `design.assignment_mechanism` and `column.measurement_timing` for `treat` — which are precisely the two facts the Russian data dictionary does not state. Eleven questions became two, and the two are the right two. It then refused `UNSUPPORTED_METHOD`; see D-104. Budgets: design 3,200/3,400, shared 2,035/2,500, tests 12,412/13,000, declarative 4,596/4,600, modules 84/86, no breached dimension. Suite 1,460 passed / 3 skipped; ruff and mypy clean.
- 2026-08-30 — D-102: The harness held the data dictionary and handed the model its filename. Found by asking why the ask gate escalated at all: SC §6.2 condition 2 forbids a question until sources are exhausted, and every requirement row in every live run reported `ev:kaggle/dataset/description` as `not_offered`. Intake had captured it. `harness_base._evidence` loaded the `EvidenceBundle` and returned `frozenset(str(row["evidence_id"]) for row in items)` — the ids, discarding the text on the line after loading it. `agenttask.invoke` then rendered those bare ids under `## allowed_evidence`. So a worker was shown three names and no documents, truthfully reported all three `not_offered`, `_is_resolved` could never fire, and the gate asked the user for facts this analysis had already committed. THE DOCUMENT: `wakizashe/lalonde-1986-nsw-dataset` ships a full per-column data dictionary in Russian — "re74 - Заработок в 1974 году (до вмешательства, долл.)", "re78 - Заработок в 1978 году (после вмешательства, долл.)", "treat - Участие в программе NSW: 1 - участвовал, 0 - нет", "Каждый объект наблюдения это отдельный человек" — i.e. per-column meaning, units, and explicit pre/post-treatment timing. `ev:kaggle/` already classifies as `data_dictionary` (`askgate.EVIDENCE_CLASS_PREFIXES`), and nine of the eleven requirements D-101 escalated accept `data_dictionary`, so the resolution path was complete and unused. Every timing and meaning answer the operator typed by hand in D-101 is written verbatim in that document. FIX: `_evidence` returns `dict[str, str]`, id to text; `_ctx` wraps it as `frozenset(...)` for the walls; `agenttask.evidence_block` renders `id:` followed by the indented text, truncated at `EVIDENCE_CHARS = 4000` per item, and still renders bare ids for a caller with no text (estimation, presentation). The prompts needed no change: `role.v1.txt:45` already said "raise a requirement only for a fact the evidence you were given cannot settle", and the model had obeyed it exactly. LIVE PROOF on `an-2f7a1da61cb8b196`: `ev:kaggle/dataset/description` is now `evidenced` on six requirements (`dataset.sampling_mechanism`, `design.outcome_window`, `design.population_comparator`, `design.selection_variables`, `design.table_grain`, `design.unit_identity`) where every prior run reported it `not_offered` on every row; requirement rows fell 20 → 14; and the role worker cited it with `support_class: direct_source_statement` for the claim "Participation in the NSW job training program (1 for participated, 0 for not participated)", read straight out of the Russian dictionary. KNOWN WEAKNESS, deliberate: `evidence_block` renders every allowlisted item into every task prompt with a per-item cap but NO total cap and no relevance filter. Three items and ~1,300 characters here; a dataset with per-column evidence could be dozens of items across five task kinds. That, not the round trip, is the case a retrieval tool would earn back (D-100). NEXT DEFECT, root-caused not fixed: the run now dies `correction_exhausted` at `roles` on `unknown_requirement_id`, because the worker writes `{"requirement_id": "req-treat-timing", "fact_required": "column.measurement_timing"}` — it puts the REGISTERED id in `fact_required` and mints an instance id for `requirement_id`. Every invented id on record has that shape (`req:treat_timing`, `req:re78_timing`, `req-treat-timing`), so this was never a vocabulary failure, it is a field-role confusion: `requirement_id` reads as "my handle" and the (requirement_id, scope_id) pair is what actually identifies the instance. The correction that should have caught it carries `"detail": ""` — `validation.unresolved_issues` names the offending id in the path and drops any statement of what was wrong or what is legal, which is the same class of loss as this entry's own defect. Budgets: design 3,187/3,400, shared 2,000/2,500, tests 12,404/13,000, modules 84/86, no breached dimension. Suite 1,458 passed / 3 skipped; ruff and mypy clean.
- 2026-08-30 — D-101: LIVE VERIFICATION of D-100 on `an-df1edf90d8fe49c7` (NSW, `wakizashe/lalonde-1986-nsw-dataset`), Claude answering as the analyst. THE FIX WORKS; the run still ends `needs_context`, now for a different and much narrower reason. Requirement rows: TWENTY, not fifty — zero `req:*` ids, zero `::` scope ids, one row per column per requirement. Every question named its scopes: "How each column encodes its values … — for: re78, treat", "When each column was measured relative to treatment … — for: re78, treat"; `design.table_grain` and `design.assignment_mechanism` shipped their option lists (`choice:one_row_per_unit|…`, `choice:randomized|self_selected|policy_cutoff|time_of_adoption`); and `design.unit_identity` was answerable for the first time ("or that the row itself is the unit when no column does"), where D-099 had to answer `unknown` twice. TEN questions settled SEVENTEEN of the twenty rows — round 1 resolved 7 rows from 5 questions, round 2 resolved 10 more — with two `column.source_process` rows correctly `unknown_accepted`. The multi-scope settle is confirmed live: one `column.encoding` answer moved both `re78` and `treat`, and `re78=post_treatment,treat=concurrent` settled two scopes with two different values from one question. REMAINING BLOCKER, now exactly one row: `design.selection_variables` at scope `current_design` was never asked in either round and stayed `open`, so the gate hit round three and terminated `round_cap_exhausted`. ARITHMETIC: this run's workers raised ELEVEN distinct blocking requirement ids against ten question slots (5 × 2). D-100's finding counted eight for `an-4e906f2e316de6f7`, so the count is model-dependent and this run was one short — not a defect in the gate, the packet, or the answers. Reordering would not have saved it: all eleven are `blocking`, so any priority order drops one. SEPARATE ORDERING DEFECT observed and NOT fixed here: `askgate._priority` ranks (0) blocking design scope, (1) column scope, (2) everything else, so a BLOCKING relationship-scope requirement sorts below a column one — `design.selection_variables` is blocking and sorted last. It is a real defect under any cap (a blocking requirement must outrank a supporting one) and cost nothing this run only because every candidate was blocking. NEXT DECISION, for the user: either raise `MAX_QUESTIONS` from 5 (a contract constant, SC §6.2 / PRD-002 §11), or constrain how many distinct requirement ids the workers may raise, or accept that a run needing more than ten distinct blocking facts terminates. Budgets unchanged from D-100: design 3,181/3,400, shared 1,986/2,500, production total 15,199/15,500, modules 84/86, no breached dimension.
- 2026-08-30 — D-100: The ask gate is fed model-authored fiction; the round cap is correct. Found by dumping `design.context_requirements` for D-099's live run `an-4e906f2e316de6f7` instead of reasoning about the schemas. FINDING: 50 rows, of which 14 carry a `requirement_id` absent from the 17-entry `context-requirements.v1` registry (`req:re78_timing`, `req:treat_meaning`, `req:unit_identity`, `req:concept_mappings`, …); `column.meaning` exists twice for the same column under two conventions (`treat` and `lalonde_nsw (1).csv::treat`); all 50 rows carry the identical three `attempted_evidence` ids (`ev:kaggle/dataset/description`, `ev:kaggle/dataset/title`, `ua:question/text`); and the distinct REGISTERED requirement ids actually open number EIGHT, against a budget of 5 × 2 = 10. ROOT CAUSE: `grep -n "scope_id=" src` returns two hits, both reads — nothing in production mints a scope id, so both `requirement_id` and `scope_id` arrive verbatim from the model's `missing_requirements` and are written to Postgres (`shared/agenttask.py:143`) with no check against anything. Two further collapses hide the scopes that are real: `askgate.build_packet` does `unique.setdefault(req.requirement_id, req)`, throwing every other scope away instead of naming it (which is why no question ever named its column), and `harness_nodes._ask` builds `index = {row.requirement_id: row for row in frozen}` over rows unique by (requirement_id, scope_id), so one answer settles only the last-sorted scope (visible in the dump: `column.encoding` resolved at `…::treat`, still open at `…::re74/re75/re78`). RULINGS: (a) `MAX_QUESTIONS = 5` and `MAX_ROUNDS = 2` STAND — eight real questions against ten slots; the cap was never the constraint. (b) `gate()` and the SC §6.2 conditions are correct and unchanged; they were routing garbage. (c) `QuestionItemV1`/`AnswerItemV1` need NO scope field — the scopes are already in hand at both ends, so no frozen contract type is amended. (d) Auto-resolving column requirements from the semantic cards the model already wrote is FORBIDDEN and must not be attempted: `column.meaning` and `column.measurement_timing` admit `user_confirmation`/`data_dictionary`/`source_statement` but deliberately not `measured_observation` — the model's own guess is not evidence for what a column means, and with `ev:kaggle/dataset/description` reported `not_offered` only a human can close these. The gate is right to ask; the asking is what was broken. FIXES: filter and normalize at the upsert boundary (registered ids only; column scopes folded to a real manifest column via `triage.normalize_column_name`), name every scope in the question text, settle every scope one answer covers (pair schemas resolve only the columns they name), delete the `enum-choice.v1` branch of `_valid_answer` in favour of the existing `choice:a|b|c` form, and feed the committed `UserContextAnswer` payloads into the `method` task — today an answer unblocks the gate and is read by no model. Registry changes are declarative: per-column schemas for `column.measurement_timing` and `column.encoding`, explicit `choice:` option lists, and an answerable schema for `design.unit_identity` (unanswerable before: no unique single column, 15 duplicate rows, and `column-list.v1` cannot say "the row is the unit"). FUNDING, on user ruling: the fix cost +34 design lines against a production total already at 15,495/15,500, so it could not be paid for from headroom. The user chose deletion over a cap revision, and the DEAD TOOL SUBSYSTEM IS DELETED rather than wired (D-098's deferral is now resolved as removal): `src/causal/design/tools.py` (147 lines), `src/causal/shared/toolrouter.py` (110), `validators.make_causal_model_handler`, `diagnostics.make_diagnostic_handlers` and the eligibility-preview block reachable only through it (`_run_preflight`, `_preview_eligibility`, `_rule_expr`, `_group_counts`, `RULE_OPS`), plus `tests/design/test_tools.py` and the handler tests. RATIONALE: none of it had ever executed — `ToolRouter` had no production caller and `allowed_tool_ids` was never rendered into any prompt — and most handlers re-served slices of the `DesignContextManifest` the context pack already carries, so the round trip bought nothing. `registries/design-tools.v1.json` keeps all eleven rows with `registered: false` and no allowed task kinds, which is what `ToolRegistrationV1.registered` is for: the capability's identity is recorded and its absence stated, so `recipient_map()` returns an empty tuple per task kind and every envelope advertises nothing. All five design prompts now say the model has no tools. AMENDMENTS: SC §5.4 (every PRD-002 tool allowlist emptied, prior lists struck through) and PRD-002 §15 (the table retained as a specification a future tool must meet, not a description of what exists); both hashes recomputed above. STILL OUTSTANDING, deliberately not touched here: presentation's `LayoutFactTool` / `resolve_registered_layout_facts` is the same defect in PRD-005 §5.4's row — advertised, never callable — and its row is left as-is. `verify_requirement_references` stays per D-098. Budgets after: design 3,181/3,400, shared 1,986/2,500, production total 15,199/15,500, tests 12,401/13,000, production modules 84/86, no breached dimension. Suite 1,458 passed / 3 skipped; ruff and mypy clean.
- 2026-08-30 — D-099: LIVE VERIFICATION of D-098 on `an-4e906f2e316de6f7` (NSW), Claude answering the ask gate as the analyst. RESULT: real progress, still short of `ExperimentDesign`. The run cleared synthesis for the first time — previous run `an-a361e5071d9a7f02` died `correction_exhausted` there — and committed `CausalContext` and `RoleLedger` with seven role claims: treatment/`concurrent`, outcome/`post_treatment`, confounder_candidate on pre-treatment earnings/`pre_treatment`, group, time, unit_identifier. The prior run's `roles` had returned ZERO hypotheses for 8 of 10 columns. It then opened two clarification rounds and terminated `needs_context` at the §11 two-round cap. ATTRIBUTION, honestly: the unblocking came from the requirement-id rendering (fix 2), not the timing carry (fix 1). This run's four `ColumnSemanticCard`s all returned `timing: unknown` and `concept_id: null`, so `MeasurementLinkV1.timing` had nothing to carry and every link reads `unknown`; the correct role timings were derived by synthesis independently. Fix 1 is mechanically correct and idle here — the previous run's cards DID carry `post_treatment`/`concurrent`, so the field matters across runs; model output varies (10 cards then, 4 now). REMAINING BLOCKER, now quantified live: the D-081 ask-gate cluster. `design.context_requirements` holds 50 scope rows against a 10-question budget (D-081 saw 25), of which FOURTEEN are model-invented `req:*` ids — `req:re78_timing`, `req:treat_meaning`, `req:unit_identity`, `req:concept_mappings`, `req:map_participation` and others — duplicating registered requirements at the same scopes. They survive because `TaskRunner.run` upserts `result.missing_requirements` with no registry check; wall 2 only validates ids referenced in a payload, which is why D-098's rendering stopped the correction-exhaustion without stopping the invention. Two scope-id conventions coexist for one column (`column.meaning` resolved at `treat`, still open at `lalonde_nsw (1).csv::treat`), so an answer resolves one and leaves the twin open (D-081 D2). Questions still never name their column (D-081 D3): round 2 asked `column.encoding`, `column.meaning`, `column.measurement_timing` with four candidate scopes each and no way to tell which; the operator had to read the scope table out of PostgreSQL to answer, which a real user cannot do. `enum-choice.v1` still validates any space-free token (D-081 D4). `design.unit_identity` remains unanswerable because `column-list.v1` cannot say "the row is the unit" (`unique_single_columns` is empty, 15 duplicate rows). NEXT: the D-081 remediation is now the only thing between this pipeline and a first live `ExperimentDesign`; validating raised requirement ids against the registry and putting the scope into the question text are the two highest-value fixes.
- 2026-08-30 — D-098: Two context-plumbing defects fixed, found by reading the D-097 traces of live run `an-a361e5071d9a7f02` (NSW, `wakizashe/lalonde-1986-nsw-dataset`), which died `correction_exhausted`. The trace showed `intent` producing a correct frame (27 claims, treat→treatment, re78→outcome) and `semantic` producing 10 correct cards including `re78 → post_treatment` and `treat → concurrent`, then `roles` returning ZERO hypotheses for 8 of 10 columns and `timing: "unknown"` for the treatment, then `synthesis` inventing the requirement id `req_treat_timing`, wall 2 rejecting `unknown_requirement_id`, and the correction budget of 2 exhausting. The model reasoned correctly at every step; the harness starved it. FIX 1 — `MeasurementLinkV1` gains `timing: TimingClass`, populated in `compile._link` from the card. PRD-002 §9.5's data-flow routes "concepts + timing + unresolved requirements" to the role workers and its routing table says the role worker receives "approved intent, assigned concepts/relationships, timing, cited cards"; the compiler was dropping a field the contract names, so this is a §9.5 conformance fix, not a feature. FIX 2 — `TaskRunner` gains `requirements: Sequence[str] = ()` and renders a `## registered_requirement_ids` heading beside the existing `## allowed_evidence` and `## parent_artifacts`; design passes the 17 ids from `context-requirements.v1.json`. This closes the pilot class already recorded against EV-SYS-002 (D-064b/D-065c/D-067/D-068): every closed vocabulary a wall enforces must be rendered into the task context. The `causal` console script attempted this session is REVERTED — it put `runtime/composition.py` at 358 against the 350 per-module cap, and the user declined both a cap revision and the module-count raise; live runs use an inline invocation from the gitignored `.env.local` instead (verified, including the fail-closed LangSmith preflight). DEFERRED with a verified shortfall: the design tool subsystem stays dead — `ToolRouter`, `tool_allowlists`, `make_retrieval_handlers`, `make_causal_model_handler`, `make_diagnostic_handlers` and presentation's `LayoutFactTool` have no production caller (`grep -rn "ToolRouter(" src/` is empty; only `tests/design/test_tools.py:137` constructs one), so 8 registered tools are advertised in every envelope and none can be called, and `allowed_tool_ids` is never rendered into any prompt although all five templates tell the model to consult it. Wiring needs ~50-60 lines; design has 41 free and modules are at 86/86; the only genuinely dead name in design/ is `verify_requirement_references` (~6 lines, LEFT IN PLACE as a real consistency check rather than deleted, since the lines proved unnecessary). Budgets after: design 3,367/3,400, shared 2,095/2,500, runtime 738/800, tests 12,660/13,000, no breached dimension. Suite 1,474 passed / 3 skipped; ruff and mypy clean.
- 2026-08-30 — D-097: USER RULING — the trace content ban is removed. SC §10.3 and PRD-002 §20.4 banned dataset content, intermediate arrays, and model reasoning from every LangSmith trace. Provenance: the clause was authored in the initial checkpoint (`58b4d84`, 2026-08-24) before any code existed, hashed the same commit, and never revisited; the decision log carries no entry weighing it, and D-020/D-043/D-045 are all downstream consequences of treating it as settled. The user ruled it "bs [that] slipped through" (2026-08-30): V1 analyses public Kaggle datasets and the ban cost exactly the observability §10 exists to provide. NEW RULE: traces carry the complete prompt, response, reasoning, and committed artifact payloads; the only content restriction is credential scrubbing (`aws_key`, `signed_url`, `bearer`, `authority_credentials`, `api_key_assignment`) — the user's gcloud ADC and Kaggle tokens are real and can surface in an error string. A release admitting private data must restore a content restriction first. CONSEQUENTIAL CHANGES: (a) §10.4's frozen profile sets `include_thoughts` true — reasoning was never returned to the process (`gateway.py:287` passed false), so chain-of-thought could not have been traced regardless of the ban; (b) PRD-002 §20.4 re-enables auto-instrumentation, making LangGraph's own node instrumentation the source of the per-node spans §10.2 requires. FINDING THAT PROMPTED THIS: `LangSmithTracer.span()` has no caller anywhere in `src/` — §10.2 mandates one child span per node and none has ever been created; `preparation/harness.py:333`, `estimation/harness.py:202`, and `presentation/harness.py:174` hardcode `"spans_acknowledged": True` for spans that do not exist; T-010's spec wired `flush()` into ArtifactCommitter and never assigned a caller for `span()`, and T-013 did not add one. The NSW pilot ran `strict_observability=False` with no project set, so no tracer object was constructed at all.
- 2026-08-27 — D-096: BUILD COMPLETE. Close verification PASSED: one composed `causal run` on a realistic 200-row RCT fixture (4% outcome missingness, four-level strata) drives intake → design approved → preparation `prepared` → estimation `complete` (ITT 1.912, 95% CI [1.473, 2.352], brackets the designed +2.0; claim `reportable`) → presentation `complete` → `causal presentation --output-dir` exports 13 hash-verified files (summary + 4 figures × svg/png/table). The 5-row wall-10 refusal test stands beside it (honest small-sample behavior documented). tests/runtime/test_full_pipeline_close.py (70 logical); `.gitignore` gains logs/ + renders/ (RuntimeConfig default output roots). Suite 1,474 passed / 3 skipped; ruff/mypy clean; no breached dimension. All 32 tasks T-001..T-032 ACCEPTED; all 50 eval registrations present. Final: production 15,488/15,500, tests 12,664/13,000, declarative 4,596/4,600, modules 86/86, grand total 32,748/33,100. REMAINING TO LIVE-PROVEN V1 (deferred by user, 2026-08-26): the D-081 ask-gate remediation, whose exploration and rulings are preserved in the plan file and D-081; after it, the live NSW leg runs the same composed path this close proved scripted.
- 2026-08-27 — D-095: T-032 accepted — THE BUILD'S FINAL PRODUCTION SLICE (Opus agent across a session-limit death + relaunch + two user budget decisions; gates green; suite 1,473). Presentation coordinator (harness 187 + nodes 244, five §16 gates, §15 cited summary, §17.2 typed destinations, presentation.runs row terminal on every exit), §20 delivery in cli/render.py (`causal presentation` exact id+hash, empty-dir export, post-copy verification), runtime/presentation.py dispatch + full `causal run` chain, EV-P5-001..006 registered (catalog covers all 50 §10.5.2 registrations — no pending surfaces), D-094's units/labels fix (+3 estimation). e2e over real Postgres/MinIO with real PRD-003+PRD-004 coordinators and a fake curator: delivered bundle, citations resolve, qualification-beside-result, export hash-verified, refusal paths, replay clean. User declined a second cap raise (15,600) → deletion sweep found 74 net behavior-preserving lines (triplicated runtime dispatch → one guarded/view/finished set; dead ManifestCommitter; dead constants), suite unchanged. Deviations: split modules per the 350 cap; dispatch in runtime/presentation.py, delivery in cli/render.py (module cap 86/86); one figure per evidence question; §15 sections 4-5 cite the bundle (judgment carries no per-diagnostic fields); presentation.runs.state carries the §1 status. FINAL BUDGETS: production 15,488/15,500 (shared 2,097/2,500, cli 466/500, runtime 738/800, intake 1,185/1,200, design 3,359/3,400, preparation 2,988/3,000, estimation 3,286/4,000, presentation 1,372/1,500); tests 12,594/13,000; declarative 4,596/4,600; modules 86/86; suite 1,473.
- 2026-08-27 — D-094: User approved (AskUserQuestion, 2026-08-27) SC §14.1 production total 15,000 → 15,500 (grand total 32,600 → 33,100 as the sum of caps; budget_check constants updated together). Trigger: T-032's pre-code count found production at 14,999/15,000 with the smallest honest final slice projecting +455 — the per-package caps sum to 16,900 and the total was the binding cell; the implementing agent correctly wrote nothing and filed the rethink (blocked_complexity_budget per §14.1.2), whose five answers found no honest in-budget plan. Same decision authorizes the ~8-line estimation fix for the second blocker the rethink surfaced: engine.figure_data defaults units/labels to empty and nodes._figures passes neither, so every real EstimationBundle would refuse at presentation gate 1 (incomplete_figure_data) — estimation supplies units/labels from the plan's outcome scale and role labels (PRD-005 §5 condition 8 unchanged).
- 2026-08-26 — D-093: T-031 accepted (Opus slice, gates green; suite 1,462). Curator (one TaskRunner call, one-layout-tool discipline, ≤2 same-code corrections, typed inability → needs_template with plan=None so the compiler is structurally unreachable), gate-2 plan validator (nine stable codes), altair compiler (FigureSpecV1 carries no Vega-Lite — compile.figure_document builds the drawn document; spec hash over the contract payload only, environment-exact; errorband/errorbar mapped to area/rule so "no transform" is structurally true over every document node), vl-convert renderer (natural-canvas measure → slack-to-padding re-render → SVG exactly 736 / PNG exactly 1472; negative slack = text_clipped → needs_layout_revision; vendored font registered, missing font = blocker). Deviations: tests +392 vs ≤350 (spec-enumerated coverage kept; 12,307/13,000); reference-line carriers (data-defined via frozen point category = reference id; fixed via module constant — PRD names no carrier); 150 px axis gutter constant to make exact-2× achievable. Budgets: presentation 941/1,500; declarative 4,554/4,600 (T-032 has 46); modules 83/86.
- 2026-08-26 — D-092: T-030 accepted (Opus slice, gates green; suite 1,427). Presentation substrate: contracts 270 + catalog 134 (presentation 404/1,500), visualization catalog 171 declarative (4 profiles, 5 templates reused via allowed-profile lists, finite bounds throughout), 5 artifact rows (registry 56), Noto Sans vendored 2,049,096 bytes sha256-verified against the SC pin from commit ec626514. Deviations: scope 404 vs task 360 and tests 316 vs 250 (no ceiling breached); template fixtures/fixture_results empty until T-031 renders them; theme_hash = hash of the version string (no theme asset exists; recorded, not fabricated); §5 condition 13 = one caller-supplied observability boolean; text/encodings/choices as keyed maps (validator enforces key sets). DECLARATIVE 4,511/4,600 — the remaining 89 lines must hold T-031's prompt and T-032's EV-P5 rows; both specs' declarative allowances are cut to ≤45 each. OFL license: fetched, sha256 cee9892f…c061a verified, vendored alongside the font with `assets/` added to budget_check's excluded prefixes (vendored third-party assets are byte-reported, not line-counted, per §14.1 — checker aligned to the contract it measures).
- 2026-08-26 — D-091: Composed-chain remediation (three micro-slices, one Opus agent, isolated worktree; merged this commit): (1) D-090 fix — PreparedFrameBundleV1 carries the prepared-stage postrepair diagnostic reports (+1 preparation contracts, +1 nodes — 350/350 exact), estimation entry reads them (bundle wins over record). (2) RULING: estimation entry accepts `not_computable` for preparation's rough pre-checks via a SEPARATE PREPARED_APPROVED_HANDLING constant at the entry-policy site only — estimation's own walls/ceiling keep frozenset(); justified because every pack pre-check id has its rigorous counterpart computed inside estimation as a required diagnostic. Implementing 16 duplicate rough diagnostics in preparation rejected (cap-infeasible, redundant under V1-mech). (3) RULING: zero-cardinality diagnostics are COMPUTED facts — rct baseline_balance emits covariate_count 0 instead of an empty harvest (adapter-level fix; engine's empty→failed guard deliberately kept — it catches silently-empty harvests); other adapters swept, none affected. Composed `causal run` now clears entry and walls 9; final fixture-scale stop is wall 10 `sensitivity_not_terminal` (leave-one-cluster-out cannot refit 4 rows — honest LinAlgError, pinned). The composed happy path needs a realistic-sized dataset = the close verification's job.
- 2026-08-26 — D-090: T-029 accepted (Opus slice, gates green; suite 1,371) — PRD-004 WAVE COMPLETE: T-022..T-029 all ACCEPTED; four-pack scripted e2e (run_estimation → `complete`, handoff accepted, rerun replay) green for rct/aipw/did/sharp_rdd. All four D-089 open items closed: runtime four-adapter map + estimate() dispatch (+105 runtime); compile_plan merges approved design facts with REQUIRED_FACTS fail-closed (cutoff reaches the plan); cross-fit objects committed via engine.CrossFitFit (no pack import in the coordinator); four-pack e2e parametrized. TWO latent cross-stage defects the wiring exposed, both fixed: input_types never emitted binary/time, and pack estimator-input roles were unassignable from PRD-002's closed RoleName vocabulary (§4 condition 6 could only ever admit RCT) → registered per-pack estimator_role_aliases (+2 declarative). Deviations: dispatch lives in failures.py (composition 345/350); "recorded handoff" read as terminal prepared outcome per D-037; DESIGN_STATUS restructured net-zero; EV-P4 rows byte-faithful, compact layout, amendment/pilot notes inline; catalog pending clause deleted (sweep fully enforced). OPEN ITEM → close verification: the composed `causal run` chain ends `failed/entry_validation_failed` code `postrepair_diagnostic_unhandled` — the scripted design fixture names required post-repair diagnostics PRD-003 does not record as `pass`; chain wiring proven, fixture not estimable; root-cause at close. Budgets: estimation 3,279/4,000; runtime 702/800; cli 435/500; declarative 4,304/4,600; tests 11,584/13,000; modules 78/86.
- 2026-08-26 — D-089: T-027 and T-028 accepted from parallel isolated worktrees (D-073 pattern); merged tree gates green: 1,357 passed, ruff/mypy clean, no breaches — estimation 3,237/4,000, tests 11,369/13,000, modules 78/86. T-027 deviations: aipw.py 345 vs ≤310 (spec baseline was stale after T-026's +661); fold Ledger makes wall-6 receipts measured not asserted; one registered standardization recipe per fold (identity when undeclared); no E-value number (qualification + influence summary carry the design-carried confounding caveat); a real bug caught by its own branch test (profile override silently re-ran the primary GLM — fixed, override wins). T-028 deviations: pyfixest 0.60 ships NO sunab() — Sun–Abraham built as the saturated i(rel_time, cohort, ref=-1) event study + registered cohort-share delta-method aggregation over the library's own vcov; repeated cross-section refused under BOTH DiD profiles (unit FE saturation aborts pyfixest pre-fit); not-yet-treated pool = never-treated ∪ last cohort; rdd placebo branch fails visibly on narrow support. OPEN ITEMS ROUTED TO T-029: (a) src-side adapter factory map (rct registration lives only in the e2e test fixture); (b) compile_plan drops the approved RDD cutoff — approved design facts must merge into estimator_parameters (adapter currently falls back to 0.0: correctness, blocks wave close); (c) coordinator-side commit of cross-fit assignment/prediction restricted objects; (d) four-pack parametrization of the coordinator e2e; T-029 test line ≤100 → ≤350 (rebalance, wave projection ≈12,620/13,000).
- 2026-08-26 — D-088: T-026 accepted (Opus slice, gates green; suite 1,324) — the estimation stage RUNS: scripted RCT e2e prepared → `complete` EstimationBundle → PRD-005 handoff accepted by the T-006 gate; exactly one gateway construction (claim task); pyfixest parity vs hand-computed values; rerun replay clean; invalidating-diagnostic path blocks the handoff. Deviations: estimation +661 vs +590 (2,351/4,000; wave projection ≈3,201 — fine); nodes.py at exactly the 350 cap (NOTHING further lands there; T-029 keeps out) with shared node plumbing moved into harness.py (318); adapters are factories bound to the committed mask; RCT pack gains multiplicity_policy_id holm_step_down.v1; pyfixest lacks CR2 → CRV1/CRV3/HC2/HC3 substitution recorded per item in method_quantities; polars→pandas via numpy boundary (no pyarrow in pins); walls 14/15 replay wall 13 against the frozen validator; atomicity exits at wall 7 inside the estimate node; D-086 ruling landed (shared +2: also_produced_by; conflict envelopes stamp the true producer, tested); preparation e2e fixture parameterized (+13 test lines) instead of duplicated. Budgets: estimation 2,351/4,000; shared 2,097/2,500; declarative 4,225/4,600; tests 10,800/13,000; modules 75/86.
- 2026-08-26 — D-087: T-025 accepted (Opus slice, gates green; suite 1,313). Deviations: ClaimJudgmentDraftV1 added as the model's return type (SC §5.4 names it) with ClaimJudgmentV1 = draft + lineage so no model echo supplies parents/refs/versions/outcome; §16.3 narrative fields at judgment level, per-item keeps status/ceiling/effect/numerics/citations; schema-default tuples with the deterministic validator (not the schema) enforcing ordering/ceiling/citations/qualifications; wall-13 shape failures ride the single stable code claim_exceeds_ceiling with detail (one-code correction discipline per §16.2); ClaimJudgment registry row appended (T-022 had not; counts 50→51); declarative +83 vs note +50. Budgets: estimation 1,690/4,000 (cap exact); declarative 4,221/4,600; tests 10,396/13,000; modules 73/86.
- 2026-08-26 — D-086: T-024 accepted (Opus slice, gates green; suite 1,298). Deviations: engine 268/harness 222 (rebalanced inside the unchanged +490 scope delta); tests +486 vs ≤350 (ratio 0.99, wave projection 11,754/13,000 still fits); DesignConflict registry row corrected on the PARENT fields (required [] / optional adds EstimationContextManifest) not the producer — producer_component is a scalar the shared committer stamps from; the estimation conflict's revision rides the schema's preparation_revision field (read as producing-stage revision); mask table keyed by the 12 registered pack rule ids; 6 comparison-rule ids over 4 generic comparisons + descriptive_only; run_diagnostics consumes the adapter harvest map (absent → not_computable, empty → failed, always terminal); EV-P4 catalog sweep carries a pending clause T-029 deletes. ARCHITECT RULING for T-026: shared registry model gains additive `also_produced_by: tuple = ()`; commit/envelope validation accepts producer ∈ {producer_component} ∪ also_produced_by and stamps the ACTUAL producing component; DesignConflict row lists estimation-harness there — a false producer stamp is not acceptable audit metadata. Budgets: estimation 1,440/4,000; declarative 4,138/4,600; tests 10,154/13,000; modules 72/86.
- 2026-08-26 — D-085: T-023 accepted (Opus slice, gates green; suite 1,244). Deviations: compile_plan takes the context-manifest ref as a third argument (self-reference underivable); entry mirrors preparation's two-struct EntryInputs/EntryPolicy shape with entry_codes exported for wall 1; aipw pack gains registered fold_assignment_rule_id `stratified_by_treatment_and_cluster` (+1 declarative — compile_plan may not invent defaults); outcome_scale from pack registration falling back to estimand id; prepared_frame_schema_id = estimator_input_schema id (the §4-cond-6 identity, none minted); DesignConflictDraftV1 redeclared in estimation (no cross-stage import) field-compatible with design-conflict.v1; 20 rules not 22 (two folds to hold walls.py at 220, all 15 walls covered); wall 4 consumes precomputed structure_codes. Confirms the D-084 ruling: T-024 widens the DesignConflict registry row's producer to estimation-harness. Budgets: estimation 950/4,000; declarative 4,087/4,600; tests 9,668/13,000; modules 70/86.
- 2026-08-26 — D-084: T-022 accepted (Opus slice, all gates green; suite 1,191). Deviations recorded: EvidenceBundleV1 registers as `EstimationEvidenceBundle`/`estimation-evidence-bundle.v1` (intake owns `EvidenceBundle`); tests 366 vs ≤300 (14 payload-model round-trips; scope 9,331/13,000, no breach); declarative +672 vs ≤+410 (artifact rows cost 357) — declarative 4,001/4,600, ~599 left: WATCH ITEM — T-023 rules registry targets ~100, the Wave-2 visualization catalog must land compact or a further §14.1 cell goes to the user; contract-field folds per the agent's list (shared bases, embedded contrast result in SensitivityResult, 3-tuple evidence_bundles, no payload_hash duplicate); three registry-count assertions 36→50; EstimationBundle forward-names ClaimJudgment as parent (T-025 registers it). ARCHITECT RULING for T-024: the DesignConflict registry row's producer widens to include estimation-harness (D-052 correction idiom) — SC §3.1 already names PRD-004 a producer; no new artifact type.
- 2026-08-26 — D-083: PRD-004 Amendment 1 ("V1-mech"), user-approved with the build plan. Same knife as D-076, two parts, no guarantee changed: (a) no LangGraph in estimation — the stage has no interrupt and one model call (claim review), so it runs as a plain sequential coordinator with D-035 artifact-replay restart; diagnostics/sensitivities execute sequentially in registered order (the ≤8-concurrency clause is an upper bound, V1 uses 1); EV-P4-010's restart leg re-scopes to rerun replay; graph_thread_id kept for trace threading. (b) Artifact consolidation mirroring D-057: Diagnostic/Sensitivity/FigureData/Uncertainty bundles merge into one generic EvidenceBundleV1 (kind + ordered refs + status counts); per-item uncertainty inlines on PrimaryContrastResult (§6.3 already lists the fields); shared ValidationReport wall bound widens 10 → 15 for the fifteen §18 walls. SC §1/§8/§9/§15 aligned (LangGraph is now PRD-002-only). Estimation stack installed at the §23 pins in the same commit.
- 2026-08-26 — D-082: User approved (plan approval, 2026-08-26) the SC §14.1 ceiling revision for the final build wave: tests 9,000 → 13,000; production modules 68 → 86; declarative 3,500 → 4,600; grand total 26,000 → 32,600 (sum of revised caps). Production total 15,000 and every per-package ceiling unchanged. Priced from measured reality per D-054: repo test ratio 0.84 test-lines/production-line over the remaining ~4,130 production lines; estimation needs ~660 declarative (pack manifest, wall rules, artifact rows, claim prompt) and 12 modules; presentation needs the visualization catalog and ~5 modules. Same decision records the build order: PRD-004 wave (T-022..T-029, V1-mech coordinator per the pending Amendment 1) then PRD-005 (T-030..T-032); the D-081 ask-gate remediation is DEFERRED by explicit user decision — close verification for these waves is the scripted pipeline; the live NSW leg stays parked behind the deferred fix. `tools/budget_check.py` constants updated in the same commit.
- 2026-08-26 — D-081: PRD-003 wave CLOSED on its scoped verification; live-leg findings recorded for the next wave. Scripted close PASSED in-suite: intake → design approved (T-013 A9 ordering) → preparation `prepared` → PRD-004 handoff accepted by the T-006 gate, four method packs, rerun replay, conflict/not_runnable/guard paths, satisfied-contract path — suite 1,065 green, all budgets inside ceilings. The LIVE NSW pilot leg (an-6099b96cc6955d3b) did NOT reach preparation: design terminated `needs_context` at the two-round ask cap. Nine PRD-002 ask-gate defects diagnosed (all pre-existing T-012/T-013 behavior, none PRD-003 scope): D1 cap arithmetic (25 scope-rows vs 2×5 questions); D2 an answer resolves only the last-sorted scope of its requirement id (harness_nodes builds a by-requirement-id dict); D3 column-scope questions never name the column (correctness hazard for timing answers); D4 enum-choice.v1 ships no option list and validates any token; D5 DesignOutcome.open_requirement_ids append-only and wrong; D6 model-invented requirement ids duplicate registry ids at the same scopes (nothing validates raised ids against the registry), doubling the ask budget; D7 no recovery path from `needs_context` (terminal, next_command null, no revision-bump exists); D8 the gate's round_cap_exhausted reason and unresolved pairs are computed then dropped (no event); D9 duplicate user_interrupt.created per resume. Also structural: `design.unit_identity` is unanswerable when the row itself is the unit (column-list.v1 cannot say so). REMEDIATION = first item of the next wave (PRD-002 scope, design 3,359/3,400 — headroom 41; several fixes are small, D1/D6/D7 need design decisions). Answer files preserved at ~/.claude/jobs/fd3c8c72/tmp/answers-r{1,2}.json; outcome designoutcome:an-6099b96cc6955d3b:099f3b82f6dc485d.
- 2026-08-26 — D-080: empty-plan path fixed (closes the D-078 open finding). `ExecutionReceiptBundleV1.receipts` may be honestly empty (a satisfied contract compiles no item); `execute_node` skips preview/executor for a zero-item plan and builds the outcome directly — walls 5/6 and readiness pass vacuously-but-checked; `PlanPreviewV1` keeps `min_length=1` (no preview exists for an empty plan). One e2e test pins `prepared` + empty bundle + open handoff on a clean source. Suite 1,065 green; preparation 2,995/3,000; tests 8,965/9,000.
- 2026-08-26 — D-079: T-020 accepted. Deviations: JSON carries no comments, so a top-level `_header` string records the format decision (D-010), the EV-P4/EV-P5 scoping (34 + 16 = the §10.5.2 total of 50), and the derived-field disclosure; `kind` and `case_budget` are derived (the tables state neither — kind from each row's dominant evaluator, case_budget as the §10.5.2 ceiling); pilot-note placement follows the committed PRD-002 §20.7 table where the spec's hints misnamed rows (intent = EV-P2-002, slots = EV-P2-003); EV-SYS-003's nine fixture focuses vs the eight-case cap recorded as a case note; EV-P3-001/003/004/007 carry inline Amendment 2 notes.
- 2026-08-26 — D-078: T-013 Amendment 9 accepted (closes the D-077 open finding). The design harness now binds the two PRD-003 §4 entry facts before the design commit, mirroring the proven e2e fixture pattern: the DeliveryCapacityCheck's D-031 identity is precomputed from its own payload and injected as `capacity_check` in ExperimentDesign (the capacity preflight is one shared pure function so the precomputed and committed payloads agree by construction), and PreRepairFeasibilityReport commits before the design and rides as its optional parent (registry already permitted it; nothing declarative changed). Design 3,359/3,400. The live scripted leg now runs intake → design → approval → preparation → prepared with the PRD-004 handoff opened. NEW OPEN FINDING (micro-slice in flight): an honest empty PreparationPlan is unrepresentable — PlanPreviewV1 requires ≥1 item, so a source that already satisfies the contract crashes `internal_error` (guarded, exit 4) instead of reaching `prepared`; EV-P3-001's satisfied-contract reading requires the typed path.
- 2026-08-26 — D-077: T-019 accepted (three Opus slices + two remediation passes, all gates green). Deviations, committed-model-wins or architect-ruled: preparation dispatch lives in `runtime/failures.py` (composition.py at the 350 cap; docstring records ownership); no separate PreparationOutcomeView — PreparationRunResult maps onto the existing DesignOutcomeView render protocol; crash re-entry starts preparation_revision n+1 (the schema's UNIQUE (analysis_id, preparation_revision) forbids reuse), so revision-bearing payload recommits are not literal no-ops; the DiD `pre_treatment_only` numeric-median target routes to DesignConflict `no_registered_resolution` (no registered pre-period-mask derivation exists; `strategy_operation` also stops the entry gate advertising it); `packs.py` folded into `plans.py`; five error classes unified under `PreparationError`; executor's three protocols merged into `FrameStore`. The deterministic compiler exposed five latent defects the model path had masked, all fixed: permitted_operation_ids carried strategy ids not operation ids (and the aipw cross-fit recipe had the same hole), `categorical_missing_encoding` allowed only fit scope `none`, three pack ids named unregistered operations, `PreparationTaskContextV1` and the executor priors/skip branch were dead under Amendment 2/D-035 and are deleted. OPEN FINDING (blocks the live pipeline leg, fix = T-013 Amendment 9 in design scope, 63 lines headroom): the live design harness can never satisfy preparation's entry gate — `DeliveryCapacityCheck` is committed after `ExperimentDesign` so the design payload's `capacity_check` is None (`capacity_binding_mismatch`; the scripted fixture proves the D-031 precomputed-identity pattern fixes it), and `PreRepairFeasibilityReport` is never a parent of `ExperimentDesign` (`prerepair_diagnostic_missing`). Two runtime tests pin today's refusal explicitly so the fix surfaces as one labelled assertion change.
- 2026-08-26 — D-076: PRD-003 Amendment 2 ("V1-mech"), user-approved after the T-019 v1 budget breach (staged work measured preparation 3,553/3,000 and nodes.py 542/350 with runtime/CLI/tests unwritten). Two mechanisms removed from V1, no guarantee changed: (a) the PreparationAgent is deferred — every V1 gap carries its contract-pinned resolution (derivation id, strategy id → operation, evidenced sentinel mapping), so the plan compiler maps gaps to items directly and anything without a unique resolution is a typed DesignConflict to PRD-002's ask gate (§2.29/§2.16); the pilot's recurring lesson (closed vocabularies the harness knew) reaches its endpoint here — the harness knows the whole answer. (b) No LangGraph in this stage — no interrupts exist, so restart is D-035 artifact replay via a plain sequential coordinator; graph_thread_id kept for trace threading. SC §1/§5.4/§9 aligned; EV-P3-003/004 re-scoped deterministic; EV-P3-007 restart leg = rerun replay. T-019 spec reissued as v2 (this consumed the wave's ONE rethink); tests ≤320 FIRM unchanged. User directives recorded: architect coordinates, Opus agents implement in small slices, each agent held under ~20% of its context window.
- 2026-08-26 — D-075: T-018 accepted. Deviations: tests +467 vs the ≤380 line (entry-gate fixture cost irreducible; rethink already consumed on the density pass that brought production +871→+719 and tests 609→467) — REBALANCE: T-019 tests ≤320, T-020 ≤80 (400 available); a genuine T-019 shortfall blocks per §14.1.2 and goes to the user. Wall 6's handoff_readable encodes PRD-003 §20's thirteen conditions against a caller-supplied readiness map; EntryPolicy carries caller-supplied imputation_strategy_ids/question_id/parser_profile_id (PRD-002 payloads lack them — T-019's composition supplies them); permitted_repair_columns derived as roled−protected−forbidden; conflicting-op check is direct-edge fail-closed; row_membership_change detected structurally; table-wide gaps under a __table__ pseudo-column; all rules user_resolvable=false; manifest commit via a ManifestCommitter Protocol satisfied by shared ArtifactCommitter.
- 2026-08-26 — D-074: T-016/T-017 accepted from parallel isolated worktrees; merged tree gates green (997 passed, ruff/mypy clean, budget warning/no breaches). Recorded deviations, all committed-model-wins or consolidation: eligibility_rule_vocabulary reader lives in packs.py; canonical hashes wrap arrays in one-key mappings; FrameArtifactV1 carries locator+hash+closed 6-dtype schema; §9.3 with no registered rule → unresolved_conflict; parse policy = typed cast failures on parse_critical columns (optional-cell failures are warnings; ragged files raise unparsable_source); operations take (frame, item, OperationContext) with scalar-only parameters (lists/maps as canonical-JSON strings); executor takes caller-supplied expected_inputs and narrow FrameWriter/FrameReader/ObjectWriter Protocols; fitted params live only in object payloads (never receipts/messages — tested); row invariance asserted structurally + frozen hash stamped; method-pack diagnostics registered with implementation=method_pack and an extra-impl hook for T-019; conftest gained MemoryObjects + a MinIO startup-race retry. Budget after merge: preparation 1,753/3,000; shared 2,063/2,500; tests 8,133/9,000 (T-018 ≤380, T-019 ≤380, T-020 ≤100 just fit); declarative 2,870/3,500; modules 62/68.
- 2026-08-26 — D-073: T-015 accepted with recorded deviations: (a) the spec's `public.causal_migrations` ledger never existed (architect error from a stale note) — 0006 follows the real apply_migrations pattern with IF NOT EXISTS idempotency + re-apply test; (b) contracts 295/plans 203 exceed the spec's per-module targets but sit far under the binding 350 cap and the 630 task line (preparation 593); (c) tests +788 vs the +360 plan — ratio 1.14 is below the repo norm, but tests now 7,361/9,000: remaining tasks are capped at T-016 ≤420, T-017 ≤400, T-018 ≤380, T-019 ≤380, T-020 ≤100 to stay inside D-058's ceiling; (d) frames classed `restricted` like KaggleCapture (they carry row data); DesignConflictV1 inherits its draft so the stamped-fields relationship is structural. Wave parallelism: T-016 and T-017 run as isolated-worktree Opus agents; the architect merges branches and re-runs full gates on the merged tree.
- 2026-08-26 — D-072: User directive: build the rest of the project breadth-first; live micro-adjustment of each PRD deferred to a later pass. Step-2 pilot closed as proven-to-the-ask-gate (the parked an-6099b96cc6955d3b run's approval leg folds into wave-close verification). Wave acceleration: T-015 solo, then T-016 ∥ T-017 (disjoint write sets, both depend only on the substrate), then T-018 → T-019 → T-020. Per-task mechanical gates (pytest/ruff/mypy/budget) are retained; they are the safety that makes deferral sound.
- 2026-08-26 — D-071: Wall red-team over the real run-10 context (committed manifest, evidence bundle, parents, registries): forged evidence citation → wall 2 `unresolved_evidence`; self-citing claim → caught at the ClaimV1 model validator itself ("an inference cannot cite itself as evidence"), surfacing as wall 1 with detail; forged parent → wall 2 `uncommitted_parent`; invented requirement id → wall 2 `unknown_requirement_id`. Two probes passed the intent walls and are recorded as EV-P2 eval-fixture candidates for T-020: (a) an invented `candidate_columns` entry is not in `_COLUMN_KEYS` — it is neutralized downstream by construction (triage folds candidates against the real inventory; a fake column can never bind to data) but a wall-2 catch would be tighter; (b) the epistemic-laundering check (`_claim_issues`) runs at wall 4+, above intent's wall-3 cap, so laundered intent claims ride until consumed at the next stage.
- 2026-08-25 — D-070: Pilot finding 11: the run reached terminal needs_context WITHOUT asking the user — the ask gate refused every blocking requirement as `sources_unexhausted` because models left `attempted_evidence` empty (SC §6.2 condition 2 requires terminal statuses over consulted sources; the field's contract was never in a prompt). Fix per T-013 Amendment 8 (declarative + test only): every template requires one attempted_evidence entry per relevant allowlisted id with a status from the REAL vocabulary {empty, not_offered, unreadable, withheld, evidenced} (implementer corrected the spec's placeholder names); test binds the listed statuses to askgate.TERMINAL_STATUSES.
- 2026-08-25 — D-069: Pilot findings 10a-c (operational, deferred to T-019's runtime work): (a) a design run killed mid-flight leaves state `running` and CANNOT be resumed via `causal run` — the runtime sees no open interrupt and reports `failed` without consulting the LangGraph checkpoint (restart semantics per D-035 need a design-stage reading: new run row + deterministic replay, or true checkpoint resume); (b) `causal status` for a running design row prints no next command (SC §1.1 promises one); (c) the `stale_revision` blocker does not name the expected id. Also observed live before the kill: the correction loop now CONVERGES (an unknown_requirement_id and a schema_invalid each corrected once, tasks completed) — D-067's detail field working as intended.
- 2026-08-25 — D-068a: Amendment 7 correction — the rendered parent line carried `id hash` while the templates demanded "verbatim", so the model echoed the whole line as the id (architect's spec error). The section now renders bare artifact ids. Suite green.
- 2026-08-25 — D-068: Pilot finding 9 (last of the closed-vocabulary class): causal_synthesis exhausted on `uncommitted_parent` — the model must echo committed parent artifact ids it was never shown. Fix per T-013 Amendment 7: `TaskRunner.invoke` renders `## parent_artifacts` (id + hash per envelope ref) after `## allowed_evidence`; every template binds `parent_artifact_ids` to that section verbatim. All wall-2 reference vocabularies (columns, evidence, requirements, parents) are now hydrated into every prompt.
- 2026-08-25 — D-067: Pilot finding 8: live semantic cards failed wall 1 with IDENTICAL near-miss slot keys three times — the exact 12-key COLUMN_CARD_SLOTS vocabulary was never in the prompt, and ValidationIssueV1 carried no message, so a temperature-0 model repeated itself. Fix per T-013 Amendment 6: the semantic template lists the 12 keys verbatim (test binds the list to the constant); ValidationIssueV1 gains additive `detail` (default "") filled by shape_report from the pydantic message (200-char cap) — corrections now say WHY. Events unchanged. The recurring pilot pattern is recorded: every model failure so far was a closed vocabulary the harness knew and never told the model, compounded by detail-free corrections.
- 2026-08-25 — D-066: Pilot finding 7: many-item tasks sent the SINGLE-draft response schema, so the live model returned one bare card and the runner crashed `KeyError: 'items'` at the semantic fan-out. Fix per T-013 Amendment 5: `result_schema(draft, many=)` wraps the payload as a required `items` array when many; a many reply without a list `items` routes through the existing schema_invalid correction path (verified: the new e2e test reproduces the exact KeyError with the guard removed). Deferred, recorded for T-019: `runtime/failures.guard` catches only GatewayError/ObservabilityError — an arbitrary exception still escapes `causal run` as a traceback; broad-exception hardening lands with T-019's runtime dispatch work.
- 2026-08-25 — D-065: Pilot findings 6a/6b/6c (round 4). (a) Live Kaggle metadata nests under `info`; the flat capture contract saw nothing, so a dataset with title/subtitle/description yielded ZERO evidence items — adapter now flattens (runtime +3, T-014 A4). (b) No allowlisted id represented the user's question, so intent claims could not legally cite it — the coordinator appends `ua:question/text` (user_confirmation class) to the EvidenceBundle; the usable/partial status computation moved above the append so provider-semantics rules are unchanged (intake +3, T-008 A4). (c) Prompts never rendered the evidence allowlist nor the authored constants — the D-059 agenttask consolidation was pulled forward: `_invoke`/`_run_task` now live in `shared/agenttask.py` as `TaskRunner` (design 3,398→3,337, shared 1,863), which appends `## allowed_evidence` to every prompt; templates state `registry_version`, per-id answer schemas, and cite-or-hypothesis discipline (T-013 A4). Suite 733 green.
- 2026-08-25 — D-064: Pilot findings 5a/5b. (a) Zero-metadata datasets produced an empty structural manifest — per-column facts lived only in the measured class — so every model task saw an empty column list. `measured_index_rows` now also emits a structural `presence` row per profiled column (intake +2, T-008 Amendment 3); a profiled column's existence is structural fact independent of provider metadata. (b) Models invented requirement ids (`req-treatment-cols-1`) because the prompts never state the registered vocabulary; wall 2 rejected them and corrections could not converge. All five design prompt templates now carry the closed registered id list with usage cues, revised IN PLACE without a version bump — no eval baseline exists yet (T-020 pending); this is the recorded pre-baseline prompt revision (T-013 Amendment 3). A consistency test binds prompt-mentioned ids to the registry.
- 2026-08-25 — D-063: Pilot finding 4 (root cause of correction_exhausted): `_invoke` passed `{"type": "object"}` as the Vertex response schema, so constrained decoding at temperature 0.0 deterministically returned `{}` — the registered draft schema never reached the model, and no scripted test could see it. Live probe confirmed Vertex v1 + google-genai 2.19.0 accept a full `model_json_schema()` dict with `$defs`. Fix per T-013 Amendment 2: new `shared/agenttask.py` `result_schema(draft)` (memoized; result-envelope schema with `payload` replaced by the draft schema, `$defs` unioned, non-identical collision raises) — the planned T-019 agenttask seed; `_invoke` passes it (design delta 0 lines).
- 2026-08-25 — D-062: Pilot finding 3: `GatewayError`/`ObservabilityError` subclass `Exception`, escaped `causal run` as a raw traceback, and left the design-run row stuck `running`. Fix per T-014 Amendment 3 (runtime scope): new `runtime/failures.py` — `guard()` wraps every coordinator invocation; GatewayError → one `blocker.raised` + run `failed` + typed result (exit 4); ObservabilityError → run `failed_observability` + typed result (exit 5), committed artifacts preserved, no blocker (the flush gate's own event covers it). composition.py delegated shared row/blocker helpers (346→330); modules 51/68. Verified: gateway emits only retry events, so the runtime blocker is the failure's single one.
- 2026-08-25 — D-061: Pilot finding 2 (first live Vertex call): `generation_config.seed` is signed INT32; the frozen unsigned-32-bit derivation overflowed it (3,755,233,513 → 400 INVALID_ARGUMENT). SC §10.4 seed row revised to a non-negative 31-bit value; `derive_seed` masks with `& 0x7FFF_FFFF`; both seed fields bound `lt=2**31` (T-010 Amendment 1). Same-task determinism preserved.
- 2026-08-25 — D-060: Pilot finding 1 (first-ever live Kaggle call): Kaggle retired `GetDatasetStatus`; `kaggle==2.2.4` `api.dataset_status` returns 404 for every dataset, so live capture failed `fetch_failed` before download. Fix per T-014 Amendment 2: `LiveKaggleClient.dataset_status` resolves `currentVersionNumber` through `api.dataset_list(search=slug)` with exact-ref match; pin, protocol, and `intake/kaggle.py` unchanged; sanitization path unchanged. Consequence accepted: the capture payload's `dataset.status` records `unknown` — the provider no longer offers a status value.
- 2026-08-25 — D-059: PRD-003 wave plan (user-approved): live PRD-002 pilot first, then T-015 substrate → T-016 Phase A stabilization engine → T-017 Phase B operations/executor/diagnostics → T-018 entry gate/plan compiler/6 walls → T-019 LangGraph harness + runtime/CLI integration + PRD-004 handoff (rethink reserve; design `_run_task` moves to `shared/agenttask.py` via the D-049 pattern) → T-020 `evals/catalog.v1.yaml`. Shared reallocation (~370 lines: receipts, frame I/O, agent-task runner) under SC §14.1 shared responsibilities. Specs freeze one at a time; Opus subagents implement (D-041).
- 2026-08-25 — D-058: User approved SC §14.1 budget revisions with the PRD-003 build plan: preparation 2,000 → 3,000; production module cap 50 → 68; tests 8,000 → 9,000; declarative 3,000 → 3,500 (scope label now names evaluation catalogs). Production total 15,000 and grand total 26,000 unchanged. `tools/budget_check.py` constants updated together; wave projection: preparation ≈ 2,840, modules 66/68, tests ≈ 8,810, declarative ≈ 3,378. Set from measured PRD-002 reality per D-054, with margin so the wave avoids a T-013-style mid-task block.
- 2026-08-25 — D-057: PRD-003 Amendment 1 (V1-lite), user-approved with the build plan after explicit discussion of agent I/O styles: the PreparationAgent uses hydrated single-shot tasks (no runtime model tool loop; extends D-053 — the walls need every inspection/gap/impact report computed anyway, so hydration adds ~no code while a tool loop adds loop+surface+tests); artifact types consolidated 25 → 9 (StabilizationRecord, PreparationPlan, ExecutionReceiptBundle fold the §5.1 list; operation-level lineage with replay-recoverability replaces the per-cell ledger); walls 13 → 6 (same checks grouped); `source_row_id` = CSV hash + row number + canonical content hash (raw-byte scanner deferred). SC §5.4 PRD-003 row and §3.1 PreparedFrameBundle parents aligned. A bounded read-only pull loop requires a future user-approved amendment backed by pilot/eval evidence.
- 2026-08-25 — D-055: User approved the second design-budget revision ("approve 3400"): SC §14.1 design 2,750 → 3,400; PRD-002 criterion 39 and budget_check updated; hashes recomputed. graph.py (790) split structurally by phase into harness_base.py (state/deps/helpers, 340), harness_nodes.py (entry→method nodes, 277), graph.py (closing nodes + assembly + coordinator API, ~236) — all under the 350 module cap. Nothing was deleted; the split follows the harness's two-phase shape.
- 2026-08-25 — Environment note: the UF_HIDDEN/.pth issue recurred twice more this session; same fix. Graphviz 15.1.1 (the exact pin) is now installed locally via Homebrew.
- 2026-08-25 — D-052: Registry correction found by T-013 integration: TableSelection and DeliveryCapacityCheck lacked preparation-harness readers, which would make the §23 handoff gate refuse reader_not_allowed. Readers + preparation destinations added.
- 2026-08-25 — D-053: V1 model tasks receive fully harness-hydrated context sections; the Vertex gateway runs no tool-calling loop (AFC disabled per SC §10.4), so the ToolRouter and retrieval handlers are contract-tested but not model-driven at runtime in V1. PRD-002 §9.5 rule 3 (harness hydrates in batched requests, workers never fetch) is the governing reading. Revisit if a tool loop is added to the gateway.
- 2026-08-25 — D-054: T-013 implementation landed complete and green (650 tests; e2e approved design runs; durable interrupt/resume; real dot 15.1.1 — amends D-042: the pinned Graphviz version is now installed locally). Budget actuals: design 3,338/2,750; graph.py 790/350. The pre-code rethink was already consumed, so the breach is terminal per SC §14.1.2. Options for the user: approve design 2,750→3,400 plus a structural split of graph.py into ≤350-line modules (compaction to ~3,200 planned under that approval), or reduce PRD-002 scope. My projections under-estimated the harness twice (T-012 targets ~40% under; graph.py 2.8×) — recorded so the PRD-003/004 budget expectations get set from measured stage-harness reality, not optimism.
- 2026-08-25 — D-051: User explicitly approved the SC §14.1 budget revision design 2,500 → 2,750 ("yes I approve", 2026-08-25). SYSTEM-CONTRACT §14.1 row, PRD-002 acceptance criterion 39, and tools/budget_check.py updated together; governing-document hashes recomputed above. Production total stays 15,000 and binds on actual lines. T-013's blocked_complexity_budget is lifted.
- 2026-08-25 — D-049: T-013 preparation moves (commit 6ac88b9): ToolRouter/tool_allowlists → shared/toolrouter.py (identical enforcement for PRD-003/004/005 per SC §5.4); CatalogReader/ProductsReader/SqlCatalogReader + FrameSource/BytesFrameSource/CsvObjectFrameSource → shared/readers.py (PostgreSQL/object access is a shared responsibility; PRD-003 reads the same surfaces). Design 2,454 → 2,310.
- 2026-08-25 — D-050: PRD-002 V1 dispatches worker batches sequentially inside the ≤8 cap (§9.1 permits parallelism, does not require it); removes parallel-dispatch wiring from the T-013 plan. Recorded with the T-013 pre-code ComplexityRethinkV1, which still projects 2,685–2,705 against the 2,500 design ceiling → T-013 is blocked_complexity_budget before feature code per SC §14.1.2. The user decides: approve design 2,500→2,750 (one §14.1 cell; production total 15,000 unchanged and binding) or reduce PRD-002 scope (no cut exists inside the frozen PRD). No code was written for T-013 beyond the D-049 moves.
- 2026-08-25 — D-048: Common validation machinery moved to `src/causal/shared/validation.py` (issue/report/rule models, declarative-rule loader, strict payload parsing, evidence-class resolution, claim self-citation, shape wall, cycle check). SC §14.1 names "common validation" a shared responsibility; PRD-003/004 validators are its next consumers. This was T-012's consumed rethink after the design scope hit 2517/2500. Same pass: migration 0005 adds scope_id to the context_requirements key; askgate's answer-schema vocabulary extended to the registry ids (free-text.v1, column-list.v1, enum-choice.v1, duration-window.v1, level-map.v1, timing-class.v1, mapping-list.v1) so registry requirements are actually askable; askgate evidence-class table deduped onto shared.
- 2026-08-24 — D-047: T-011 resolutions. Batch sizing constant BATCH_COLUMN_LIMIT=4 with size max(4, ceil(n/8)) — pins the 8-batch cap for any column count. supported_estimands[0] is each pack's default (ITT for RCT); multiplicity policy encoded as an RCT invalidation rule. StructuralFieldV1.dtype is declared/undeclared (catalog views expose slot availability, not values) and ordinal is the catalog's deterministic per-table rank. Tool event ids are monotonic per envelope. The T-011 rethink was consumed converting make_retrieval_handlers (88 lines) into module-level handlers over a frozen RetrievalContext.
- 2026-08-24 — D-045: LangSmith 0.11.0 has no per-batch ingest acknowledgement. The flush gate's verifiable contract is: client constructed with tracing_error_callback (fires once per exhausted ingest attempt) + post-flush tracing-queue-drain check; either signal raises ObservabilityError(flush_unacknowledged). Limits (sampled-out batches invisible; detection per flush, not per run) are recorded at the flush site. Span create/close failures map to flush_unacknowledged. SDK retries in google-genai default to 5 attempts; the gateway pins attempts=1 explicitly per SC §10.4.
- 2026-08-24 — D-044: T-009 Amendment 1. RoleName has 18 values (17 roles + unknown); an approved DesignOutcome requires all five refs (design, frame contract, graph view, capacity check, approval) non-None; the registry gains an eighteenth design row `DesignApprovalDecision` so the SC §11.1 verbatim CLI decision commit does not fail closed, and `DesignApproval` lists it as an optional parent. Registry now 26 rows.

## Checkpoint log

One line per checkpoint commit: date, commit subject, what state it freezes.

- 2026-09-03 — T-034: visible failed sensitivities count as terminal evidence; composed runtime fixture reaches its real claim ceiling; actual Groupon AIPW and state-panel DiD both deliver with qualifications; suite 1,490 green.
- 2026-08-26 — `ea1c4da` D-082 §14.1 ceilings; `ccd47b7` PRD-004 Amendment 1 (D-083); T-022..T-029 specs frozen as a batch (this commit) — the PRD-004 wave is fully specced: 12 estimation modules ≈3,130 projected, slices contracts→spine→substrate→judge→RCT e2e→AIPW→DiD/RDD→runtime+evals; per-slice budgets sum estimation 3,130/4,000, tests +2,600, declarative +720; the wave shares ONE §14.1.2 rethink.

- 2026-08-24 — `4f7cbaf` T-001: environment (exact §14 pins, uv.lock), package skeleton, budget checker + 36 tests.
- 2026-08-24 — `bd6c985` T-002: canonical serialization + hashing + ArtifactEnvelopeV1/HandoffManifestV1 + 40 tests. Full suite 76 green.
- 2026-08-24 — Environment note: repo lives under ~/Documents (likely iCloud-synced); macOS set UF_HIDDEN on venv files, which makes Python 3.12 silently skip .pth files. Cleared with `chflags nohidden`; if imports break again after re-sync, re-run: `find .venv -flags +hidden -exec chflags nohidden {} +`
- 2026-08-24 — `7b96eeb` T-003: OperationalEventV1 + emitter, 21 tests.
- 2026-08-24 — `99d0132` T-004: artifact-type registry + seeded rows, 11 tests. Full suite 107 green; budget `within_budget` (shared 355, tests 769, declarative 35, 13 modules).
- 2026-08-24 — `c2fc732` T-005: persistence + commit protocol, 14 dockerized tests. Suite 121.
- 2026-08-24 — `ec5db6d` T-006: handoff store + gate, 11 dockerized tests. Suite 132.
- 2026-08-24 — `87629ec` T-007: intake core (contracts/archive/profiler + 6 registry rows), 58 tests. Suite 181 green; budget `within_budget` (shared 745, intake 344, tests 1570, declarative 166; 18 modules).
- 2026-08-24 — `faad0c8`/`a26c270` T-008 spec frozen + two pre-implementation amendments (D-027..D-038).
- 2026-08-24 — `e613eed` T-008: Kaggle capture Protocol, field-class registry, evidence/semantic-map builders, catalog migration + 5 views, coordinator with idempotency/refusals/handoff opening, shared build_event/build_envelope. 34 tests; suite 215 green; budget `warning` (intake 1180/1200, shared 792/2500, tests 2050/8000, declarative 291/3000, largest module 329/350; 25 modules). Intake budget is nearly exhausted by design — PRD-001 is complete; nothing further lands in the intake scope except fixes.
- 2026-08-24 — Environment note: the UF_HIDDEN/.pth issue recurred mid-session and was cleared again with `find .venv -flags +hidden -exec chflags nohidden {} +`. MinIO fixture now retries on XMinioServerNotInitialized, not just connection refusal.
- 2026-08-24 — `2c98730` T-009: shared AgentTaskEnvelopeV1/AgentTaskResultV1/ContextRequirementV1/ClaimV1; design contracts/semantics/frame models; 18 design registry rows (26 total); 0004_design.sql (8 tables). 134 new tests; suite 349 green.
- 2026-08-24 — `ff4e7f8` T-010: gateway.py (250) + tracing.py (187) + committer flush gate (+6); 44 tests; suite 390 green + 2 gated live smokes. Vertex/LangSmith APIs introspected from installed pins, none guessed.
- 2026-08-24 — `d6d4cf2` T-011: packs.py/triage.py/entry.py/tools.py + method-packs/context-requirements/design-tools registries + ColumnTriageRecord row (27 types); 67 tests; suite 457 green.
- 2026-08-25 — `8ec3e94` T-012: validators (344→walls-only) + askgate + diagnostics + capacity + 3 registries + shared/validation.py + 0005 migration; 139 tests; suite 596 green.
- 2026-08-25 — `6ac88b9` T-013 prep: shared toolrouter + readers; design 2310/2500, shared 1732/2500; suite 596 green.
- 2026-08-25 — T-013 spec + rethink frozen; task BLOCKED_COMPLEXITY_BUDGET awaiting user decision (D-050).
- 2026-08-25 — `84d83cf` T-013: LangGraph design harness (16 nodes, 3 durable interrupts, §16.4 corrections, §23 handoff), compile + prompts + renderer; D-055 budget revision + module split; 54 tests; suite 650 green.
- 2026-08-25 — `97e1a65` T-014: cli/main+render (410), runtime composition (349) + live Kaggle adapter (75); 67 tests; suite 717 green + 3 gated live smokes. PRD-002 wave complete: T-009..T-014 all ACCEPTED.
- 2026-08-25 — T-013 Amendment 8 (pilot fix): attempted-evidence discipline in prompts; declarative +30; suite green.
- 2026-08-25 — T-013 Amendment 7 (pilot fix): parent-artifact hydration; suite 738 green.
- 2026-08-25 — T-013 Amendment 6 (pilot fix): slot vocabulary in semantic prompt + issue detail in corrections; suite 737 green.
- 2026-08-25 — T-013 Amendment 5 (pilot fix): many-item response schema + hardened fan-out parse; shared +8; suite 735 green.
- 2026-08-25 — Round-4 pilot fixes (T-014 A4, T-008 A4, T-013 A4): flat metadata, question evidence, allowlist hydration + TaskRunner move; design 3,337, shared 1,863; suite 733 green.
- 2026-08-25 — T-008 Amendment 3 + T-013 Amendment 3 (pilot fixes): structural presence rows (intake 1,182) + prompt requirement vocabulary (declarative 2,115); suite 729 green.
- 2026-08-25 — T-013 Amendment 2 (pilot fix): real response schema; shared 1,755, design 3,398 unchanged; suite 727 green.
- 2026-08-25 — T-014 Amendment 3 (pilot fix): typed terminal results for harness-boundary errors; runtime 479/800; suite 722 green.
- 2026-08-25 — T-010 Amendment 1 (pilot fix): 31-bit Vertex seed; suite green; SC hash recomputed at commit.
- 2026-08-25 — T-014 Amendment 2 (pilot fix): live version resolution via dataset_list; runtime 427, tests 6,354; suite 719 green.
- 2026-08-26 — SESSION CHECKPOINT v2 (compaction aid; supersedes v1 and conversation memory): [v3] Wave state — T-015..T-018 ACCEPTED (HEAD near `f3072df`; suite 1,047 green; preparation 2,472/3,000, tests 8,600/9,000, modules 65/68). AN OPUS AGENT IS IMPLEMENTING T-019 IN THE MAIN REPO NOW against frozen docs/tasks/T-019-preparation-graph-runtime.md (graph/nodes, runtime dispatch + failures hardening, CLI render, agenttask echo-field stamping; tests ≤320 FIRM — a shortfall BLOCKS and goes to the user; modules end ≤68). It runs gates but does NOT commit — on its notification the architect reviews, ledgers, commits (stage explicit paths; NEVER `git add -A` while an agent works). Then freeze T-020 from `~/.claude/jobs/fd3c8c72/tmp/T-020-draft.md` (eval catalog as JSON-syntax .yaml; tests ≤80) → implement → wave-close verification: scripted full pipeline (intake→design approved→preparation prepared→PRD-004 manifest passes the T-006 gate) plus the live pilot leg (parked interrupt an-6099b96cc6955d3b; pilot env per v1 text below). Standing: Opus writes all code; breadth-first (D-072); lean tests (D-073/D-075).
- 2026-08-26 — T-019: deterministic coordinator (harness 348, nodes 349), deterministic plan compiler, runtime dispatch + broad-exception guard + crash re-entry, CLI render; packs folded into plans; 13 new tests (309); suite 1,058 green; preparation 2,995/3,000, no breached dimensions.
- 2026-08-26 — D-076 doc freeze: PRD-003 Amendment 2 (V1-mech) + SC alignment + T-019 spec v2; hashes recomputed above; T-019 v1 staged code remains uncommitted raw material for the v2 implementation.
- 2026-08-26 — T-018: entry 251, plancompile 248, validators 220 + rules registry (90); 50 tests; suite 1,047 green.
- 2026-08-26 — `3772ea6`/`f7ddf0a` T-016 + T-017 merged: Phase A engine (stabilize 298, impact 180, shared/frames 90) and Phase B engine (operations 278, executor 214, diagnostics 179 + 2 registries); 108 new tests; suite 997 green.
- 2026-08-26 — T-015: preparation substrate (contracts 295, plans 203, packs 95, shared/receipts 97; 9 registry rows → 36; 0006_preparation.sql; overlay registry); 152 tests; suite 891 green.
- 2026-08-25 — Step 1 of the PRD-003 wave (D-057..D-059): PRD-003 Amendment 1 (V1-lite) appended; SC §5.4/§3.1/§14.1 aligned and budgets revised; budget_check constants updated; hashes recomputed. Checker `warning` (pre-existing near-cap scopes only), no breaches.
