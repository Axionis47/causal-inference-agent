// Runtime config stub for local development.
// In Docker, this file is generated by docker-entrypoint.sh at container start.
// Values here are overridden by the entrypoint script in containerized environments.
window.__CONFIG__ = {
  // Use the vite dev proxy (same-origin /api -> :8000) so the app works on
  // whatever port vite picks, with no CORS. Docker overrides this at runtime.
  API_URL: "/api",
  API_KEY: ""
};
