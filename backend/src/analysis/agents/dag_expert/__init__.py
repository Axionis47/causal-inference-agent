"""DAG Expert agent package.

Writes to state.refined_dag (a CausalDAG) — same type causal_discovery
writes to discovered_dag. The model lives in causal_discovery/output.py;
this package owns no new output type.
"""
