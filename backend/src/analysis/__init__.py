"""Analysis package: orchestrator, substrate, and specialist agents.

Public surface (filled in later steps):
- run(state): the seam from JobInput to a finalised AnalysisState.

This package is being grown incrementally. Today only substrate exists;
agents and orchestrator move in during stages 1 and 3.
"""
